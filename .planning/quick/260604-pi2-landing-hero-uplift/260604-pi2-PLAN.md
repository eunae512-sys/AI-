---
quick_id: 260604-pi2
slug: landing-hero-uplift
description: 랜딩 Hero를 1등 수준으로 고도화 (매거진 에디토리얼 결 유지)
date: 2026-06-04
status: complete
---

# Quick Task 260604-pi2: 랜딩 Hero 1등 고도화 (1차)

대상: `briq-app/components/landing/Hero.tsx`, `briq-app/lib/landing/tokens.ts`

매거진 에디토리얼 결(Aesop/Hermès/Kinfolk — 순수 타이포·절제된 팔레트)을 유지하면서
첫인상·위계·전환·신뢰를 끌어올린다. **오케스트레이션 메모:** 타이포 craft 정밀 수술이라
sonnet executor 하청 대신 orchestrator(Opus)가 직접 편집 → tsc·헤드리스 스크린샷으로 검증.

## 개선 항목

1. **한글 가짜 이탤릭 전면 제거** (최대 craft 결함)
   - 나눔명조엔 진짜 이탤릭이 없어 `font-style: italic` 지정 시 합성 기울임(싸구려)이 됨.
   - 헤드라인 `자동으로`, 필드노트 `신메뉴`/`어버이날`, 다이어리 '오늘' 요일 →
     풀-잉크·무게·하이라이트·하어라인 밑줄로 강조 교체.
   - 진짜 이탤릭은 라틴(Cormorant)에만 유지: `ON`, 타임스탬프, `Today`, 폴리오, 콜로폰.

2. **주 CTA 한글화** (한국 소상공인 전환)
   - `Begin`→`14일 무료로 시작`, `View the demo`→`데모 둘러보기`, 모바일 스티키 `Begin`→`무료로 시작`.
   - 한글은 넓은 자간·대문자 금물 → `tracking` 0.16em→0.01em, `uppercase` 해제.
   - 영문은 마스트헤드(Nav)·폴리오·섹션 마커에 액센트로 유지.

3. **정직화** (1등 신뢰도)
   - 미검증 '소상공인 200+ 가게 사용 중' 제거 → '인스타·블로그·카카오 한 곳에서'.
   - 라이브 수치 블록(Field Notes·Diary)에 '실제 운영 화면 예시' 라벨 → 시연임을 명시.

4. **대비 보정**: `INK_MUTE` `#8C8881`→`#767268` (작은 메타 라벨 가독성↑, 헤드라인 위계 유지).

## 검증

- `tsc --noEmit` 통과 (exit 0)
- dev 서버 재컴파일 OK, `GET / 200`
- 헤드리스 Chrome 스크린샷으로 렌더 확인 (헤드라인 upright·한글 CTA·예시 라벨)
