---
phase: quick-260607-w7f
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - briq-app/lib/nav.ts
  - briq-app/components/layout/RoadmapStrip.tsx
  - briq-app/components/ai-assistant/AssistantDrawer.tsx
  - briq-app/components/dashboard/Dashboard.tsx
  - briq-app/components/channels/ChannelsScreen.tsx
  - briq-app/components/settings/SettingsScreen.tsx
  - briq-app/app/(app)/clients/[id]/page.tsx
  - briq-app/components/shorts/ShortsScreen.tsx
  - briq-app/components/reels/ReelsScreen.tsx
  - briq-app/lib/queue/publish-queue.ts
  - briq-app/app/robots.ts
autonomous: true
requirements: [ROUTE-CLEANUP-1]

must_haves:
  truths:
    - "검수(review-queue)가 메인 사이드바 nav에 visible 항목으로 노출된다"
    - "삭제된 5개 라우트(pipeline/schedule/calendar/analytics/trends)로 향하는 dangling 링크가 0개다"
    - "삭제된 5개 라우트의 nav 항목이 모두 제거된다"
    - "/scheduler, /insights, /discover, /review-queue 등 canonical 라우트가 정상 렌더된다"
    - "tsc --noEmit 통과 (삭제로 인한 import 오류 0)"
    - "발행/생성/카피/이미지 로직 무손상, clients 라우트 보존"
  artifacts:
    - path: "briq-app/lib/nav.ts"
      provides: "review-queue visible nav 항목 + 5개 삭제 라우트 항목 제거"
    - path: "briq-app/components/layout/RoadmapStrip.tsx"
      provides: "STEP4 href /scheduler, STEP5 href /insights, match 배열 정리"
  key_links:
    - from: "RoadmapStrip STEP4/STEP5"
      to: "/scheduler, /insights"
      via: "href + match 배열"
      pattern: "href: \"/(scheduler|insights)\""
    - from: "lib/nav.ts navGroups"
      to: "/review-queue"
      via: "visible NavItem"
      pattern: "href: \"/review-queue\""
---

<objective>
앱 라우트 1차 정리. 페르소나 무관 안전 정리만 수행한다.

1. 검수(`/review-queue`)를 메인 nav에 visible 승격 (쐐기 핵심인데 숨겨져 있었음).
2. 고아 stub `/pipeline` 삭제.
3. legacy `/schedule` + `/calendar` → canonical `/scheduler` 병합·삭제.
4. `/analytics` → canonical `/insights` 병합·삭제.
5. `/trends` → `/discover` 통합 (trends 삭제, discover 유지).
6. nav·RoadmapStrip·robots 등 잔재 0 정합성 확보.

Purpose: 라우트 중복·레거시·고아를 제거해 IA를 단순화하고, 검수 흐름을 사용자에게 노출.
Output: 5개 라우트 디렉터리 + 4개 orphan 컴포넌트 삭제, 링크 재지정, nav/RoadmapStrip 정리.

제약: clients(대행사) 절대 삭제 금지. 발행/생성/카피/이미지 로직 무손상. dangling 금지 — import/href되는 컴포넌트·라우트는 재지정 후 삭제.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@./CLAUDE.md
@briq-app/lib/nav.ts
@briq-app/components/layout/RoadmapStrip.tsx

<orphan_facts>
grep으로 확정된 사실 (실행자는 재확인만, 발명 금지):
- CalendarScreen: app/(app)/calendar/page.tsx 에서만 import. → calendar 라우트 삭제 시 컴포넌트도 orphan, 삭제 안전.
- ScheduleScreen: app/(app)/schedule/page.tsx 에서만 import. → orphan, 삭제 안전.
- AnalyticsScreen: app/(app)/analytics/page.tsx 에서만 import. → orphan, 삭제 안전.
- TrendsScreen: app/(app)/trends/page.tsx 에서만 import. /shorts 등 다른 곳 import 없음 → orphan, 삭제 안전 (trends 삭제 보류 불필요).
- /pipeline: page.tsx 단일 파일, 전용 component 없음 (components/pipeline 부재). nav.ts:73 외 참조 없음 → 삭제 안전.
- canonical /scheduler, /insights, /discover, /review-queue 디렉터리 모두 존재 확인됨.
</orphan_facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: lib/nav.ts — 검수 visible 승격 + 5개 삭제 라우트 항목 제거</name>
  <files>briq-app/lib/nav.ts</files>
  <action>
1) import 라인(1-13)에 `ClipboardCheck` 아이콘 추가. 현재 import 목록에 check 계열 아이콘이 없으므로 lucide-react import에 `ClipboardCheck,` 한 줄 추가 (예: `Compass,` 다음 줄).

2) navGroups 첫 그룹(items, 43-46행)에 검수 항목을 visible로 추가. Insights 다음(46행 뒤)에 추가:
```
      { label: "검수", href: "/review-queue", icon: ClipboardCheck, badge: { count: 3, tone: "amber" } },
```
(쐐기 흐름상 운영 그룹에 둔다. badge count는 데모용 정적값으로 기존 Campaigns/Scheduler 패턴과 동일.)

3) HIDDEN_ROUTES(71-87행)에서 다음 5개 항목 라인 전체 삭제:
   - 73행 `{ label: "Content Pipeline", href: "/pipeline", ... }`
   - 75행 `{ label: "Analytics", href: "/analytics", ... }`
   - 80행 `{ label: "Calendar", href: "/calendar", ... }`
   - 81행 `{ label: "Schedule (legacy)", href: "/schedule", ... }`
   - 84행 `{ label: "Trends", href: "/trends", ... }`

4) HIDDEN_ROUTES에서 82행 `{ label: "Review queue", href: "/review-queue", ... }` 항목 삭제 (이제 visible 그룹으로 이동했으므로 중복 제거).

남길 hidden 항목: content-distribution, automation, brand-kit, channels, threads, brand-tone, reviews, discover, clients.
  </action>
  <verify>
    <automated>cd briq-app && grep -nE 'href: "/(pipeline|schedule|calendar|analytics|trends)"' lib/nav.ts | grep -c . | grep -qx 0 && echo NAV_CLEAN || (echo NAV_DANGLING; exit 1)</automated>
  </verify>
  <done>nav.ts에 /review-queue visible 1개, 삭제 5개 라우트 항목 0개, ClipboardCheck import됨.</done>
</task>

<task type="auto">
  <name>Task 2: RoadmapStrip + 링크 재지정 (schedule/calendar→scheduler, analytics→insights)</name>
  <files>briq-app/components/layout/RoadmapStrip.tsx, briq-app/components/ai-assistant/AssistantDrawer.tsx, briq-app/components/dashboard/Dashboard.tsx, briq-app/components/channels/ChannelsScreen.tsx, briq-app/components/settings/SettingsScreen.tsx, briq-app/app/(app)/clients/[id]/page.tsx, briq-app/components/shorts/ShortsScreen.tsx</files>
  <action>
RoadmapStrip.tsx:
- 49행 `href: "/schedule",` → `href: "/scheduler",`
- 50행 `match: ["/schedule", "/calendar", "/review-queue"],` → `match: ["/scheduler", "/review-queue"],` (삭제될 /schedule·/calendar 제거)
- 57행 `href: "/analytics",` → `href: "/insights",`
- 58행 `match: ["/analytics", "/trends", "/discover", "/reviews"],` → `match: ["/insights", "/discover", "/reviews"],` (analytics·trends 제거, discover·reviews 유지)

AssistantDrawer.tsx:
- 13행 `{ icon: Calendar, label: "이벤트 카피", href: "/calendar", tone: "amber" },` → href를 `"/scheduler"` 로. (icon Calendar import는 그대로 둬도 무방.)

Dashboard.tsx:
- 792행 `{ href: "/calendar", label: "Calendar" },` → `{ href: "/scheduler", label: "Scheduler" },`

ChannelsScreen.tsx:
- 327행 `<StudioLink href="/calendar" title="콘텐츠 캘린더" sub="예약·발행 큐" />` → `<StudioLink href="/scheduler" title="발행 스케줄러" sub="예약·발행 큐" />`

SettingsScreen.tsx:
- 257행 `href="/analytics"` → `href="/insights"`

clients/[id]/page.tsx:
- 222행 `<Link href="/schedule" ...>` → href를 `"/scheduler"` 로.

ShortsScreen.tsx (grep로 추가 발견된 2건):
- 513행 `<Link href="/schedule">` → `<Link href="/scheduler">`
- 741행 `<Link href="/schedule">` → `<Link href="/scheduler">`

주의: `/schedule?date=${iso}` 쿼리 형태는 CalendarScreen 내부(273/358/391행)에만 있고, CalendarScreen은 Task 3에서 컴포넌트째 삭제되므로 재지정 불필요.
  </action>
  <verify>
    <automated>cd briq-app && grep -rIn 'href="/schedule"\|href="/calendar"\|href="/analytics"\|href: "/schedule"\|href: "/calendar"\|href: "/analytics"\|href={"/schedule"}\|href={"/calendar"}' components/layout/RoadmapStrip.tsx components/ai-assistant/AssistantDrawer.tsx components/dashboard/Dashboard.tsx components/channels/ChannelsScreen.tsx components/settings/SettingsScreen.tsx 'app/(app)/clients/[id]/page.tsx' components/shorts/ShortsScreen.tsx | grep -c . | grep -qx 0 && echo LINKS_RETARGETED || (echo STILL_DANGLING; exit 1)</automated>
  </verify>
  <done>모든 활성 링크가 /scheduler·/insights로 재지정. RoadmapStrip STEP4=/scheduler, STEP5=/insights, match 배열에 삭제 라우트 없음.</done>
</task>

<task type="auto">
  <name>Task 3: 라우트·orphan 컴포넌트 삭제 (pipeline/schedule/calendar/analytics/trends)</name>
  <files>briq-app/app/(app)/pipeline, briq-app/app/(app)/schedule, briq-app/app/(app)/calendar, briq-app/app/(app)/analytics, briq-app/app/(app)/trends, briq-app/components/schedule, briq-app/components/calendar, briq-app/components/analytics, briq-app/components/trends</files>
  <action>
삭제 전 orphan 최종 확인 (이미 PLAN orphan_facts에서 확정됐으나 실행자는 한 번 재grep):
```
cd briq-app
grep -rIn "ScheduleScreen\|CalendarScreen\|AnalyticsScreen\|TrendsScreen" app components lib | grep -v "components/schedule/\|components/calendar/\|components/analytics/\|components/trends/\|app/(app)/schedule/\|app/(app)/calendar/\|app/(app)/analytics/\|app/(app)/trends/"
```
→ 출력이 0줄이어야 삭제 진행 (자기 page.tsx 외 import 없음 확인). 0줄이 아니면 중단하고 보고.

확인 후 다음 디렉터리 삭제 (git rm -r):
- app/(app)/pipeline
- app/(app)/schedule
- app/(app)/calendar
- app/(app)/analytics
- app/(app)/trends
- components/schedule
- components/calendar
- components/analytics
- components/trends

clients·discover·scheduler·insights·review-queue 는 절대 건드리지 않는다.
  </action>
  <verify>
    <automated>cd briq-app && test ! -d "app/(app)/pipeline" && test ! -d "app/(app)/schedule" && test ! -d "app/(app)/calendar" && test ! -d "app/(app)/analytics" && test ! -d "app/(app)/trends" && test ! -d components/schedule && test ! -d components/calendar && test ! -d components/analytics && test ! -d components/trends && test -d "app/(app)/clients" && test -d "app/(app)/scheduler" && test -d "app/(app)/insights" && test -d "app/(app)/discover" && test -d "app/(app)/review-queue" && echo DELETED_OK || (echo DELETE_MISMATCH; exit 1)</automated>
  </verify>
  <done>5개 라우트 + 4개 orphan 컴포넌트 디렉터리 삭제됨. canonical 5개 라우트 보존.</done>
</task>

<task type="auto">
  <name>Task 4: 잔재 텍스트 정리 (robots.ts, 코멘트·토스트) + tsc + 빌드 정합성</name>
  <files>briq-app/app/robots.ts, briq-app/components/reels/ReelsScreen.tsx, briq-app/lib/queue/publish-queue.ts</files>
  <action>
1) robots.ts 8행 disallow 배열에서 삭제된 라우트 제거:
   `"/schedule"`, `"/trends"`, `"/analytics"`, `"/calendar"` 4개 문자열 삭제.
   canonical 추가: `"/scheduler"`, `"/insights"` 를 disallow에 추가 (내부 운영 화면이므로 기존 정책과 동일하게 색인 차단).
   최종 disallow 예: `["/api/", "/dashboard", "/clients", "/cardnews", "/reels", "/scheduler", "/insights", "/review-queue", "/brand-tone", "/brand-kit", "/reviews", "/settings"]`

2) ReelsScreen.tsx 토스트 문구(링크 아님, 정직성 차원):
   - 214행 `... — /schedule 에서 확인` → `... — 스케줄러에서 확인`
   - 230행 `... — /schedule 에서 발행 시간 지정` → `... — 스케줄러에서 발행 시간 지정`

3) publish-queue.ts 2행 코멘트:
   `... → /schedule 에서 동일 데이터 표시` → `... → 스케줄러에서 동일 데이터 표시`

(content-distribution/page.tsx:6 의 "scheduled" 는 라우트 아님 — 건드리지 말 것. AnalyticsScreen 코멘트는 컴포넌트째 삭제되어 무관.)

4) 전체 검증:
```
cd briq-app && ./node_modules/.bin/tsc --noEmit -p tsconfig.json
```
오류 0이어야 한다 (삭제로 인한 import 오류 없음).
  </action>
  <verify>
    <automated>cd briq-app && ./node_modules/.bin/tsc --noEmit -p tsconfig.json && grep -rIn "/schedule\b\|/calendar\b\|/analytics\b\|/trends\b\|/pipeline\b" app components lib | grep -Ev "/scheduler|scheduled|/schedule-" | grep -E "href|push|from |import |hidden|match|disallow" | grep -c . | grep -qx 0 && echo ALL_CLEAN || (echo RESIDUE_FOUND; exit 1)</automated>
  </verify>
  <done>tsc 0 오류. robots·코멘트·토스트에서 삭제 라우트 잔재 0 (scheduler 오탐 제외).</done>
</task>

</tasks>

<verification>
1. tsc: `cd briq-app && ./node_modules/.bin/tsc --noEmit -p tsconfig.json` → 0.
2. 잔재 grep (오탐 제외):
   `cd briq-app && grep -rIn "/schedule\b\|/calendar\b\|/analytics\b\|/trends\b\|/pipeline\b" app components lib | grep -Ev "/scheduler|scheduled|/schedule-" | grep -E "href|push|from |import |hidden|match|disallow"` → dangling 0.
3. nav 확인: review-queue visible, 삭제 5개 nav 항목 부재 (Task 1 grep).
4. dev 서버 헤드리스 응답 (실행자 선택 — dev 서버 3000 가동 시):
   canonical 200/정상 렌더: /scheduler /insights /discover /review-queue /dashboard /campaigns.
   삭제 404 기대: /schedule /calendar /analytics /trends /pipeline.
   ```
   cd briq-app && for p in scheduler insights discover review-queue dashboard campaigns; do code=$(curl -s -o /dev/null -w "%{http_code}" http://localhost:3000/$p); echo "$p $code"; done
   for p in schedule calendar analytics trends pipeline; do code=$(curl -s -o /dev/null -w "%{http_code}" http://localhost:3000/$p); echo "$p $code (expect 404)"; done
   ```
5. 워크트리 미사용.
</verification>

<success_criteria>
- review-queue가 메인 nav visible.
- 5개 라우트 디렉터리 + 4개 orphan 컴포넌트 삭제.
- 모든 활성 링크 /scheduler·/insights로 재지정 (dangling 0).
- tsc 0 오류.
- clients·discover·scheduler·insights·review-queue 보존, 발행/생성 로직 무손상.
</success_criteria>

<output>
After completion, create `.planning/quick/260607-w7f-route-cleanup-1/260607-w7f-SUMMARY.md`
</output>
