---
phase: quick-260608-1x0
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - briq-app/components/landing/Hero.tsx
  - briq-app/components/landing/Sections.tsx
  - briq-app/components/dashboard/Dashboard.tsx
autonomous: true
requirements: [LANDING-HONESTY-01]
must_haves:
  truths:
    - "랜딩에 '발행/응답이 무조건 자동'이라고 단정하는 문구가 없다 (검수 후/제안 맥락 명시)"
    - "생성·작성의 '자동'은 그대로 유지되어 제품 매력이 죽지 않는다"
    - "카카오 발송·Studio+ 인스타의 자동은 그대로 인정된다"
    - "미검증 단정 수치(평균 3분 회신 등)가 완화 또는 제거된다"
    - "예시 미리보기 라벨은 유지된다"
  artifacts:
    - path: "briq-app/components/landing/Hero.tsx"
      provides: "헤드라인·Field Notes 산문·schedule status 정직화"
    - path: "briq-app/components/landing/Sections.tsx"
      provides: "비교표·운영 흐름·자동화 영역·FAQ 정직화"
    - path: "briq-app/components/dashboard/Dashboard.tsx"
      provides: "댓글 응답 라벨·수치 정직화"
  key_links:
    - from: "SSOT §3 채널별 정직 라벨"
      to: "랜딩 카피"
      via: "발행/응답=반자동(검수 후), 카카오·Studio+=자동, 생성=자동 유지"
---

<objective>
랜딩 정직성 마감. SSOT(BRIQ_오퍼확정_1장.md §3)의 채널별 정직 라벨에 맞춰
"자동" 과장을 정정한다. 핵심 구분:
- 생성/작성(카드뉴스·릴스·캡션·블로그 본문)의 "자동"은 **참 → 유지**.
- 발행·응답을 무조건 "자동"으로 단정한 것은 **과장 → 반자동(검수 후)/제안으로 정정**.
- 단 카카오 비즈메시지 발송·Studio+ 인스타 Graph API 직결은 **자동 인정**.
- 미검증 단정 수치("평균 3분 회신")는 완화/제거.

Purpose: 과장광고 리스크 제거 + CLAUDE.md "정직성" 철칙 준수. 제품 매력(생성 자동)은 보존.
Output: 3개 컴포넌트의 줄단위 카피 교체. 로직·디자인 토큰·구조 무손상.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@./CLAUDE.md

핵심 제약:
- 한글 가짜 이탤릭 금지(나눔명조에 italic 금물). 강조는 색·무게·HL.
- `word-break: keep-all` 보존. 변수/JSX 구조/컴포넌트/로직 무손상.
- 카피 텍스트만 교체. className·style·props 시그니처 변경 금지.
- [확정 필요] 항목(가격·무료체험·페북/틱톡 등)은 이번 범위 밖 — 건드리지 말 것.

SSOT §3 확정 라벨:
- 인스타 발행: 반자동(기본) / 자동(Studio+). 댓글·DM: 반자동(제안 답변→사장 클릭 발송).
- 네이버 블로그: 본문 자동 작성 + 반자동 발행(게시는 사용자 확인).
- 카카오: 자동(공식 비즈메시지).
- "자동"은 공식 API로 지속가능한 것만. 나머지는 "반자동(검수 후 발행)".
</context>

<tasks>

<task type="auto">
  <name>Task 1: Hero.tsx 정직화 (헤드라인 · Field Notes 산문 · status)</name>
  <files>briq-app/components/landing/Hero.tsx</files>
  <action>
아래 4건을 정확히 교체한다. 각 항목은 해당 텍스트만 바꾸고 JSX 구조·className·style·컴포넌트(`<Quiet>`, `<Stamp>`, `<em>`)는 그대로 둔다.

[1-1] 헤드라인 @125-128 — "SNS는 자동으로 굴러갑니다"가 풀자동 오해.
"사장님은 장사만, / SNS 는 …" 구조와 punch를 보존하면서 "검수 한 번"의 정직 뉘앙스로 교체.
`<Quiet>` 톤다운 + INK 강조 단어의 대비 구조는 유지(한글 이탤릭 금지).

OLD (line 125-128):
```
              <Quiet>SNS 는{" "}</Quiet>
              {/* 한글 강조는 이탤릭(가짜 기울임) 대신 풀-잉크 + 무게로 — 주변 muted 와의 대비가 강조를 만든다 */}
              <span style={{ color: INK, fontWeight: 500 }}>자동으로</span>
              <Quiet>{" "}굴러갑니다.</Quiet>
```
NEW:
```
              <Quiet>SNS 는{" "}</Quiet>
              {/* 한글 강조는 이탤릭(가짜 기울임) 대신 풀-잉크 + 무게로 — 주변 muted 와의 대비가 강조를 만든다 */}
              <span style={{ color: INK, fontWeight: 500 }}>검수 한 번으로</span>
              <Quiet>{" "}굴러갑니다.</Quiet>
```

[1-2] Field Notes 산문 @236 — "오늘 자동 발행은 2건 완료" → 검수 맥락 반영.
이미 @217-219/@265 예시 라벨 안이라 수치(2건)는 유지. "자동 발행" 단정만 정정.
"댓글·DM 자동 응답은 ON" → "댓글·DM 답변 제안은 ON" 으로 같이 정정.

OLD (line 236-237):
```
              오늘 자동 발행은 <Stamp>2</Stamp> 건 완료되었습니다. 댓글·DM 자동
              응답은 <em className="italic" style={{ fontFamily: SERIF_LATIN }}>ON</em>{" "}
```
NEW:
```
              오늘 검수 후 발행은 <Stamp>2</Stamp> 건 완료되었습니다. 댓글·DM 답변
              제안은 <em className="italic" style={{ fontFamily: SERIF_LATIN }}>ON</em>{" "}
```

[1-3] 미검증 수치 @238 — "평균 3분 안에 회신" 통계 단정 완화.
"보통 몇 분 안에" 로 톤다운. `<Stamp>` 컴포넌트 제거하고 일반 텍스트로(수치 단정 회피).

OLD (line 238):
```
              — 평균 <Stamp>3분</Stamp> 안에 회신합니다. 이번 주 진행 중인
```
NEW:
```
              — 보통 몇 분 안에 답변안을 띄워드립니다. 이번 주 진행 중인
```

[1-4] Figure detail @247 — "comments · DMs · 3 min" 미검증 수치 → 라벨만 정정.
"Auto-reply" 라벨과 "3 min" 단정 완화.

OLD (line 247):
```
              <Figure label="Auto-reply" value="ON" italic detail="comments · DMs · 3 min" />
```
NEW:
```
              <Figure label="Reply drafts" value="ON" italic detail="comments · DMs · suggested" />
```

[1-5] WEEK schedule status @537 — "토요일 영업 안내 / status: 자동".
Story 채널(인스타 스토리)은 SSOT상 인스타=반자동이므로 "자동" status가 자동발행으로 오해됨.
"예약"으로 교체(StatusMark는 "예약"=SAGE dot 이미 지원, 일관됨).
참고: StatusMark의 case "자동"(@499) 분기는 다른 status가 안 쓰면 dead지만 로직이므로 제거하지 말 것 — status 값만 교체.

OLD (line 537):
```
  { day: "토", time: "09:00", channel: "Story", title: "토요일 영업 안내", status: "자동" },
```
NEW:
```
  { day: "토", time: "09:00", channel: "Story", title: "토요일 영업 안내", status: "예약" },
```
  </action>
  <verify>
    <automated>cd briq-app && ./node_modules/.bin/tsc --noEmit -p tsconfig.json 2>&1 | head -5; grep -n "자동으로\|자동 발행\|자동 응답" components/landing/Hero.tsx</automated>
  </verify>
  <done>tsc 0 에러. Hero.tsx에 "자동으로"/"자동 발행"/"자동 응답" 잔존 0. 헤드라인 "검수 한 번으로", 산문 "검수 후 발행"·"답변 제안", 수치 "보통 몇 분 안에"로 교체됨.</done>
</task>

<task type="auto">
  <name>Task 2: Sections.tsx 정직화 (비교표 · 운영 흐름 · 자동화 영역 · FAQ)</name>
  <files>briq-app/components/landing/Sections.tsx</files>
  <action>
아래 건을 정확히 교체. 텍스트만 변경, 구조·style·map 키 보존. 생성=자동 유지, 발행/응답만 정정.

[2-1] ProblemSection 비교표 @102-103 — 댓글 응답·발행 픽 정정. (캡션 자동 @101은 생성=참, 유지)

OLD (line 102-103):
```
          { num: "03", label: "댓글·DM 응대 시간", out: "브랜드 톤 학습된 자동 응답" },
          { num: "04", label: "발행 시간·요일 결정", out: "채널별 반응 데이터 기반 자동 픽" },
```
NEW:
```
          { num: "03", label: "댓글·DM 응대 시간", out: "브랜드 톤 학습된 답변 제안 (검수 후 발송)" },
          { num: "04", label: "발행 시간·요일 결정", out: "검수 후, 채널별 반응 데이터로 예약 발행" },
```

[2-2] ToneFeature 운영 흐름 @227 — step03 body "동시 발행 / 자동 응답" 정정.
헤드라인 @210 "사장님은 승인만 하시면 됩니다", @222 "'이대로 발행'"은 이미 정직 → 유지.
카카오 발송은 자동 인정 가능하나, 인스타·블로그가 같이 묶여 "동시 발행"으로 자동 단정되므로 "검수 후"로.

OLD (line 226-227):
```
            title: "최적 시간에 자동 발행",
            body: "채널별 반응 좋은 시간에 인스타·블로그·카카오로 동시 발행. 댓글·DM 도 자동 응답.",
```
NEW:
```
            title: "검수 후, 최적 시간에 발행",
            body: "확인을 누르면 채널별 반응 좋은 시간에 인스타·블로그로 예약 발행, 카카오는 공식 메시지로 발송. 댓글·DM 은 톤 맞춘 답변 제안.",
```

[2-3] CalendarFeature 자동화 영역 리스트 @269-275 — 발행/응답/재발행 항목에 "검수 후"/"제안" 명시.
헤드라인 @262 "사장님이 따로 안 해도 되는 것"은 유지(준비를 대신한다는 의미는 참).
생성 항목(블로그 본문 @270, 검색어 분석 @274, 성과 노트 @276)은 자동 유지.

OLD (line 269-275):
```
          "매일 인스타 한 컷 자동 발행",
          "주 1회 네이버 블로그 1500자 본문",
          "댓글·DM 브랜드 톤 자동 응답",
          "반응 좋은 게시물 60일 후 자동 리믹스 재발행",
          "리뷰 자동 스토리 큐레이션",
          "동네 검색어 분석 → 다음 주 콘텐츠 큐 자동 갱신",
          "발행 시간 채널별 최적 자동 픽",
```
NEW:
```
          "매일 인스타 한 컷 자동 생성 → 검수 후 발행",
          "주 1회 네이버 블로그 1500자 본문 자동 작성",
          "댓글·DM 브랜드 톤 답변 제안 (검수 후 발송)",
          "반응 좋은 게시물 60일 후 자동 리믹스 → 검수 후 재발행",
          "리뷰 스토리 자동 큐레이션",
          "동네 검색어 분석 → 다음 주 콘텐츠 큐 자동 갱신",
          "발행 시간 채널별 최적 자동 픽 (검수 후 예약)",
```

[2-4] FAQ @376 — 채널 답변을 SSOT 라벨대로 풀어쓰기.

OLD (line 376):
```
            a: "인스타 자동 발행 · 네이버 블로그 자동 작성·발행 · 카카오 채널 메시지 발송 까지. 채널 연결은 1분.",
```
NEW:
```
            a: "인스타는 검수 후 발행(상위 플랜은 자동 발행) · 네이버 블로그는 본문 자동 작성에 게시 확인 한 번 · 카카오 채널 메시지는 공식 API로 발송. 채널 연결은 1분.",
```

[2-5] CasesSection @339 — "자동 발행" 라벨. 이미 @300-304 "운영 예시" 라벨 안이지만 "자동 발행"이 단정.
"주 N회" 운영량 라벨이므로 "발행" 만으로 충분.

OLD (line 339):
```
                자동 발행
```
NEW:
```
                주간 발행
```

참고: @303 "실제 고객사가 아닌, 업종별 운영 예시입니다." 및 @317 "Example" 라벨은 이미 정직 → 유지. CasesSection 가게(@308-310)는 Example 라벨 안이라 추가 정정 불필요.
  </action>
  <verify>
    <automated>cd briq-app && ./node_modules/.bin/tsc --noEmit -p tsconfig.json 2>&1 | head -5; grep -n "자동 발행\|자동 응답" components/landing/Sections.tsx</automated>
  </verify>
  <done>tsc 0 에러. Sections.tsx에 검수 맥락 없는 "자동 발행"/"자동 응답" 잔존 0. 생성 항목("자동 작성"·"자동 생성"·"자동 큐레이션"·"자동 갱신")은 유지. FAQ가 SSOT 라벨대로 풀어쓰기됨.</done>
</task>

<task type="auto">
  <name>Task 3: Dashboard.tsx 정직화 (댓글 응답 라벨 · 수치)</name>
  <files>briq-app/components/dashboard/Dashboard.tsx</files>
  <action>
앱 내부 대시보드의 미검증 수치·자동 응답 단정을 SSOT 일관성 위해 정정.
StatusBlock props(label/value/sub) 시그니처 변경 없이 문자열만 교체.

OLD (line 217):
```
          <StatusBlock label="댓글 자동 응답" value="ON" sub="평균 3분 회신" />
```
NEW:
```
          <StatusBlock label="댓글 답변 제안" value="ON" sub="검수 후 발송" />
```
  </action>
  <verify>
    <automated>cd briq-app && ./node_modules/.bin/tsc --noEmit -p tsconfig.json 2>&1 | head -5; grep -n "자동 응답\|평균 3분" components/dashboard/Dashboard.tsx</automated>
  </verify>
  <done>tsc 0 에러. Dashboard.tsx에 "댓글 자동 응답"·"평균 3분 회신" 잔존 0. "댓글 답변 제안 / 검수 후 발송"으로 교체됨.</done>
</task>

</tasks>

<verification>
1. `cd briq-app && ./node_modules/.bin/tsc --noEmit -p tsconfig.json` → 0 에러.
2. grep 잔존 점검:
   `cd briq-app && grep -n "자동 발행\|자동 응답\|자동으로" components/landing/*.tsx`
   → 검수 맥락 없는 단독 등장 0. (생성 "자동 작성/생성/큐레이션/갱신/픽"은 허용.)
3. dev 서버(3000) 헤드리스 Chrome 스크린샷으로 랜딩(/) 육안:
   - 히어로 헤드라인 "검수 한 번으로 굴러갑니다"
   - 운영 흐름 step03 "검수 후, 최적 시간에 발행"
   - 자동화 영역 리스트에 "검수 후 발행"·"답변 제안" 노출
   - FAQ "인스타는 검수 후 발행…" 노출
   - 예시 라벨(An illustrative preview / 운영 예시 / Example) 유지
4. 워크트리 미사용. 직접 작업.
</verification>

<success_criteria>
- 발행·응답을 무조건 "자동"으로 단정하는 랜딩 문구 0 (검수 후/제안 명시).
- 생성·작성의 "자동" 표현 보존 (제품 매력 유지).
- 카카오 발송·상위 플랜 인스타 자동은 인정.
- 미검증 단정 수치(평균 3분 회신) 완화/제거.
- 예시 라벨 유지. tsc 0 에러. 디자인 토큰·구조·로직 무손상.
</success_criteria>

<output>
After completion, create `.planning/quick/260608-1x0-landing-honesty/260608-1x0-SUMMARY.md`
</output>
