---
phase: quick-260606-14v
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - briq-app/lib/cardnews/hook-patterns.ts
  - briq-app/lib/cardnews/hook-generator.ts
autonomous: true
requirements: [COPY-NATURALNESS]
must_haves:
  truths:
    - "비음식·무형 업종에서도 '오늘은 어떤 한 끼을/하루을 골라드릴까요?' 같은 조사 비문이 안 나온다 (을→를 자동 보정)"
    - "비시즌 캠페인(단골·리뷰·트렌드)의 일반 캡션 풀에 '내년에야 다시 나와요' 같은 시즌 단정이 섞이지 않는다"
    - "playful 무드 캡션 오프너에 '저장 안 하면 손해예요' 류 저장강박 FOMO 클리셰가 없다"
    - "일반 캡션 오프너에 'AI 클리셰 저장각' + '이번 시즌 한정' 시즌 단정이 동시에 박히지 않는다"
  artifacts:
    - path: "briq-app/lib/cardnews/hook-patterns.ts"
      provides: "조사 헬퍼로 보정된 question 후크"
      contains: "을(c.v.experienceWord)"
    - path: "briq-app/lib/cardnews/hook-generator.ts"
      provides: "시즌 단정·FOMO 클리셰 제거된 일반 캡션 풀"
  key_links:
    - from: "hook-patterns.ts question 후크"
      to: "korean-particles.을"
      via: "import"
      pattern: "import.*을.*korean-particles"
---

<objective>
카드뉴스 자동 생성 카피 풀에서 **전수 정독으로 식별된 진짜 어색한 4건만** 정밀 교정한다.
과교정 금지 — 멀쩡한 라인은 손대지 않는다. 어색함 근거를 항목마다 명시했고, 실행자는 아래 old/new 를 **그대로 적용만** 한다.

식별 결과 (전수 정독):
- **reels/hook-pool.ts** — 전수 정독 결과 모두 단문 완결형 자연스러운 사장님 구어체. **교정 0건**(과교정 방지).
- **hook-patterns.ts** — `HOOK_PATTERNS_BY_TYPE` 만 사용자 표시(hook-generator 가 import). `CTA_BY_INTENSITY`(DM 'OPEN'·놓치지 마세요 포함), `INDUSTRY_SEQUENCE`, `INDUSTRY_TONE_MAP` 은 **어디서도 렌더 안 됨(dead/내부 가이드)** → grep 으로 확인, 절대 손대지 않음. 표시되는 `HOOK_PATTERNS_BY_TYPE` 에서 조사 비문 1건만 교정.
- **hook-generator.ts** — 일반 풀(캠페인 종류 무관 사용)에 섞인 시즌 단정·FOMO 클리셰 3건 교정.

Purpose: 비음식/무형/비시즌 캠페인에서 문장이 깨지거나 거짓이 되는 케이스 제거.
Output: 교정된 hook-patterns.ts · hook-generator.ts. tsc 0 · jiti 프로브로 6업종 육안 확인.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md

<interfaces>
조사 헬퍼 — briq-app/lib/utils/korean-particles.ts (이미 hook-generator 는 import 중, hook-patterns 는 미import):
```typescript
export function 을(word: string): string;  // 받침 있으면 "을", 없으면 "를"
// "한 끼" → "한 끼를", "한 잔" → "한 잔을", "하루" → "하루를", "한 접시" → "한 접시를"
```

experienceWord 업종별 값 (vocabFor): restaurant="한 끼", cafe="한 잔", dessert="한 접시", beauty="한 시술", stay="하루", local="한 컷".
→ 현재 `${experienceWord}을` 은 "한 끼을 / 하루을 / 한 접시을" 비문 발생(끝 글자 받침 없음).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: hook-patterns.ts question 후크 조사 비문 교정 (을→를 자동)</name>
  <files>briq-app/lib/cardnews/hook-patterns.ts</files>
  <action>
**근거(비문, 최우선):** `오늘은 어떤 ${experienceWord}을\n골라드릴까요?` 에서 experienceWord 가 "한 끼"(restaurant)·"하루"(stay)·"한 접시"(dessert) 처럼 받침 없는 값이면 "한 끼을 / 하루을 / 한 접시을" 같은 깨진 조사가 발행물에 그대로 노출된다. `을()` 헬퍼로 받침 판별 자동화.

**오직 이 두 군데만 수정. 다른 라인·dead export(CTA_BY_INTENSITY 등) 절대 손대지 말 것.**

교체 1 — import 추가 (line 6 `import type { Brand } from "@/types";` 바로 다음 줄에 삽입):
- old:
```
import type { Brand } from "@/types";
```
- new:
```
import type { Brand } from "@/types";
import { 을 } from "@/lib/utils/korean-particles";
```

교체 2 — question 후크 조사 보정 (line 161):
- old:
```
    (c) => `오늘은 어떤 ${c.v.experienceWord}을\n골라드릴까요?`,
```
- new:
```
    (c) => `오늘은 어떤 ${을(c.v.experienceWord)}\n골라드릴까요?`,
```

주의: `\n` 줄바꿈 위치·배열 구조·함수 시그니처 보존. `을()` 는 단어+조사를 한 번에 반환하므로 뒤에 따로 "을" 을 붙이지 않는다(중복 금지).
  </action>
  <verify>
    <automated>cd briq-app && ./node_modules/.bin/tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <done>tsc 0. line 161 이 `${을(c.v.experienceWord)}` 로 바뀌고 import 추가됨. "한 끼을/하루을" 비문이 "한 끼를/하루를" 로 보정됨.</done>
</task>

<task type="auto">
  <name>Task 2: hook-generator.ts 일반 풀 시즌 단정·FOMO 클리셰 3건 교정</name>
  <files>briq-app/lib/cardnews/hook-generator.ts</files>
  <action>
아래 **3건만** 교체. 모두 "캠페인 종류 무관 일반 풀"(buildCaption·MOOD_CAPTION_OPENER)에서 비롯된 문맥 부정합/클리셰. 다른 라인(특히 VALUE_SEASON_POOL·HOOK_SEASON 같은 시즌 전용 풀)은 시즌 단정이 정당하므로 손대지 말 것.

교체 1 — CAPTION_BODY_LINES 시즌 단정 (line 757)
**근거(문맥 부정합):** CAPTION_BODY_LINES 는 buildCaption 이 **모든 캠페인 종류**에 공통으로 쓴다. "내년에야 다시 나와요" 는 단골·리뷰·트렌드 캠페인에선 거짓이 됨(CLAUDE.md·브리프가 '내년에야' 를 명시 지적). 시즌 단정 제거하고 일반화.
- old:
```
  (c) => `· ${c.t.timeWord ?? "이번 시즌"} 지나면 ${c.t.subject || c.v.signature}, 내년에야 다시 나와요.`,
```
- new:
```
  (c) => `· ${c.t.subject || c.v.signature}, 생각날 때 한 번 들러주세요.`,
```

교체 2 — MOOD_CAPTION_OPENER.playful 저장강박 FOMO (line 737)
**근거(AI 클리셰·저장강박):** "저장 안 하면 손해예요" 는 CLAUDE.md 가 금지한 저장강박/FOMO 트리거. 느낌표(!)도 SaaS 시그널. playful 무드는 유지하되 강박 제거.
- old:
```
  playful: (c) => `${c.v.city} ${c.v.catShort} 중에 이건 진짜예요!\n${c.t.subject || c.v.signature}, 저장 안 하면 손해예요.`,
```
- new:
```
  playful: (c) => `${c.v.city} ${c.v.catShort} 중에 이건 진짜예요.\n${c.t.subject || c.v.signature}, 보면 또 생각나요.`,
```

교체 3 — CAPTION_OPENERS 저장각+시즌단정 (line 712)
**근거(AI 클리셰 + 시즌 단정):** CAPTION_OPENERS 는 무드 없는 브랜드의 일반 오프너(종류 무관). "저장각" 은 CLAUDE.md 가 금지한 AI 클리셰, "이번 시즌 한정" 은 비시즌 캠페인에서 거짓이 되는 시즌 단정. 둘 다 제거하고 사장님 구어체로.
- old:
```
  (c) => `${c.v.city} ${c.v.catShort} 다닐 일 있으면 저장각.\n${c.t.subject || c.v.signature}, ${c.t.timeWord ?? "이번 시즌"} 한정.`,
```
- new:
```
  (c) => `${c.v.city} ${c.v.catShort} 다닐 일 있으면 한 번 떠올려 주세요.\n${c.t.subject || c.v.signature}, 요즘 자주들 찾으세요.`,
```

주의: `${...}` 변수 자리·조사 헬퍼·`\n` 위치·배열 구조·함수 시그니처 100% 보존. 위 3개 외 라인 절대 변경 금지. ("저장각" 은 해시태그 풀(#저장각)·trigger 풀에도 등장하지만 그건 해시태그라 유지 — old_string 이 line 712 오프너 전문이라 유일 매칭됨.)
  </action>
  <verify>
    <automated>cd briq-app && ./node_modules/.bin/tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <done>tsc 0. line 757 시즌 단정 제거, line 737 FOMO 제거, line 712 저장각+시즌단정 제거. 시즌 전용 풀(VALUE_SEASON_POOL·HOOK_SEASON·CTA_SEASON)은 무변경.</done>
</task>

</tasks>

<verification>
두 task 적용 후 통합 검증 (jiti 프로브 — 이미 동작 확인된 패턴, alias 필요):

```
cd briq-app && cat > .probe.cjs <<'EOF'
const path=require('path');const {createJiti}=require('jiti');
const jiti=createJiti(__filename,{alias:{'@':path.resolve(__dirname)}});
(async()=>{const hg=await jiti.import('./lib/cardnews/hook-generator.ts');
const bm=await jiti.import('./lib/dummy/brands.ts');
for(const id of ['miokdang','roastery-1985','dolce-dessert','luna-hair','seochon-stay','forum-fashion']){
  const b=bm.getBrand(id); const c=hg.generateCardnewsCampaign('대표주제',b);
  console.log('\n==',id,b.industry,'==');
  for(const s of c.slides) console.log(`[${s.n} ${s.role}] ${(s.caption||'').replace(/\n/g,' / ')} | sub: ${s.subtext||''}`);
  console.log('  CAPTION:', (c.marketing.caption||'').replace(/\n/g,' / '));
}})().catch(e=>{console.error('ERR',e.message);process.exit(1)});
EOF
node .probe.cjs; rm -f .probe.cjs
```

육안 확인 포인트:
1. **비문 없음:** question 후크가 떠도 "한 끼을/하루을/한 접시을" 안 나오고 "한 끼를/하루를/한 접시를" 로 보정.
2. **시즌 단정 격리:** 비시즌(단골/리뷰/트렌드로 라우팅될 토픽) 캡션에 "내년에야 다시 나와요" 미출현. `inferKindFromTopic` 으로 '대표주제' 는 신메뉴 폴백이므로, 추가로 `generateCardnewsCampaign('단골 감사', b)` · `generateCardnewsCampaign('리뷰 모음', b)` 도 한 번 호출해 캡션에 시즌 단정 안 섞이는지 확인.
3. **FOMO 없음:** playful 브랜드(dolce-dessert) 캡션 오프너에 "저장 안 하면 손해예요" 미출현.
4. **변수 치환 자연성:** ${signature}/${subject}/${customerWord} 치환된 새 문구가 6업종 모두 자연스러운지.

reels hook-pool 은 이번 교정 대상 0건(전수 정독 결과 자연스러움) — 별도 프로브 불필요.
(선택) dev 서버 헤드리스 스크린샷: `cd briq-app && ./node_modules/.bin/next dev -p 3000` 후 /cardnews 확인.
</verification>

<success_criteria>
- tsc --noEmit 0 에러.
- jiti 프로브 6업종 + 단골/리뷰 토픽: 비문·시즌단정·FOMO 클리셰 미출현, 새 문구 자연 치환.
- 교정 외 라인 무변경(특히 dead CTA_BY_INTENSITY·시즌 전용 풀·영문 검색어 빌더).
</success_criteria>

<output>
After completion, create `.planning/quick/260606-14v-copy-naturalness/260606-14v-SUMMARY.md`
</output>
