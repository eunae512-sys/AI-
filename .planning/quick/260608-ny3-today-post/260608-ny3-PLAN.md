---
phase: quick-260608-ny3
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - briq-app/lib/dummy/today-shift.ts
  - briq-app/components/dashboard/Dashboard.tsx
autonomous: false
requirements:
  - TODAY-REAL-01
  - TODAY-HONEST-02
user_setup: []

must_haves:
  truths:
    - "온보딩 브랜드(signatureMenu 보유)의 today post 주제가 시그니처 메뉴 실데이터에서 도출된다"
    - "데모/시즌 폴백 브랜드의 today post 주제가 현시즌 실주제(getSeasonalTopics) 또는 brand.campaign에서 도출된다"
    - "5개 톤(editorial/minimal/warm-shop/witty/premium)은 같은 실주제를 톤별 문체로 감싸 출력한다 (주제 하드코딩 회귀 없음)"
    - "deterministicPick 결정론이 유지된다 (Math.random 없음, 같은 입력=같은 출력)"
    - "신규 가짜 수치(개수·배수·랭킹·'60개'/'+2.4배'/'저장 상위')가 생성되지 않는다"
    - "FEATURE 카드의 'Auto operations · live' 라벨이 데이터 출처(실입력 vs 시즌 추정)에 따라 정직하게 표기된다"
    - "InstagramMobilePreview·CoverStory 렌더가 무손상으로 동작한다 (TodayPost 형태 불변)"
  artifacts:
    - path: "briq-app/lib/dummy/today-shift.ts"
      provides: "실주제 주입형 톤 래퍼 today post 생성기"
      contains: "getSeasonalTopics"
    - path: "briq-app/components/dashboard/Dashboard.tsx"
      provides: "활성 브랜드의 UserBrandData 실데이터 전달 + 정직한 라벨"
  key_links:
    - from: "briq-app/components/dashboard/Dashboard.tsx"
      to: "getShopHand"
      via: "userBrands.find(b => b.id === brand.id) 의 signatureMenu/tagline 전달"
      pattern: "getShopHand\\(brand,\\s*tone"
    - from: "briq-app/lib/dummy/today-shift.ts"
      to: "getSeasonalTopics"
      via: "폴백 주제 도출"
      pattern: "getSeasonalTopics"
---

<objective>
대시보드 "오늘의 한 컷"(FEATURE / today post)의 주제를 업종×톤별 하드코딩 더미(editorialPool 등)에서 **브랜드 실데이터**(시그니처 메뉴 → 캠페인 → 현시즌 주제)로 전환한다. 톤 시스템(5종)은 "표현 래퍼"로 유지하되, 같은 실주제를 톤별 문체로 감싸 slideTitle/slideHint/caption을 생성한다. 동시에 FEATURE 카드의 "Auto operations · live" 라벨을 데이터 출처에 맞게 정직하게 정정한다(CLAUDE.md 정직성 #7).

Purpose: 현재 dessert×editorial = "오늘은 60개" 하나뿐인 풀이 사실상 영구 고정되어 실제 메뉴·시즌·캠페인과 무관하게 표시됨 → 더미를 실데이터로 오인시키는 정직성 위반.
Output: 실주제 주입형 순수함수 생성기 + 활성 브랜드 실데이터 배선 + 정직한 라벨.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/STATE.md

<interfaces>
<!-- 실데이터 소스 — 이미 존재. 새로 만들지 말 것. 직접 사용. -->

From briq-app/lib/brand/user-brand.ts (온보딩 실데이터):
```typescript
export type UserBrandData = {
  id: string;
  name: string;
  industry: Industry;
  // 사장님이 직접 입력한 가게 정보:
  tagline?: string;        // "엄마 손맛 그대로, 강남에서 30년"
  signatureMenu?: string[]; // ["갈비찜", "냉이된장국", "5첩 한정식"]
  // ...
};
```

From briq-app/lib/content/seasonal-topics.ts (시즌 실주제 풀 — 72개):
```typescript
export type Topic = { title: string; keywords: string[]; intent: string };
// season 미지정 시 현재 KST 시즌. 업종 미존재 시 restaurant 폴백.
export function getSeasonalTopics(industry: string, season?: SeasonKey, now?: Date): Topic[];
```

From briq-app/lib/content/season.ts:
```typescript
export type SeasonKey = "spring" | "summer" | "autumn" | "winter";
export function getSeasonContext(now?: Date): SeasonContext; // { month, seasonKey, seasonLabel, monthLabel }
```

From briq-app/lib/dummy/today-shift.ts (수정 대상 — 현 시그니처):
```typescript
export function getShopHand(brand: Brand, tone: VoiceTone, now?: Date): ShopHand;
// TodayPost.{slideLabel, slideTitle, slideHint?, caption, hashtags[], reasoning, ...}
// deterministicPick<T>(pool, seedStr): T  — 31-해시 결정론. Math.random 금지.
```

From briq-app/components/brand/BrandProvider.tsx (useBrand):
```typescript
// 활성 brand 는 userBrands.find(b => b.id === brandId) 에서 파생됨.
// 따라서 활성 브랜드의 실데이터 = userBrands.find(b => b.id === brand.id) 이지
// 단일 userBrand(=primary 첫 브랜드)가 아님. 멀티브랜드 대행사 정합 주의.
const { brand, userBrands } = useBrand(); // userBrands: UserBrandData[]
```
</interfaces>

<!-- 소비처 — 무손상 확인 대상 (수정 금지). TodayPost 형태 불변 유지: -->
@briq-app/components/dashboard/InstagramMobilePreview.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: today-shift.ts — 실주제 주입형 톤 래퍼로 today post 생성 전환</name>
  <files>briq-app/lib/dummy/today-shift.ts, briq-app/lib/dummy/today-shift.test.ts</files>
  <behavior>
    순수함수 설계 (Math.random 금지, deterministicPick 결정론 유지):
    - Test 1: signatureMenu=["휘낭시에"] 전달 시 → slideTitle/caption에 "휘낭시에"가 포함된다 (5개 톤 모두).
    - Test 2: signatureMenu 없음 + campaign 있음 → caption/주제가 campaign 또는 getSeasonalTopics 현시즌 주제에서 도출된다 (하드코딩 "60개" 미포함).
    - Test 3: 같은 (brand, tone, signatureMenu, now) 2회 호출 → slideTitle/caption 동일 (결정론).
    - Test 4: 어떤 입력에서도 출력 caption/slideTitle/slideHint에 신규 가짜수치 패턴(/\d+개|\+?\d+(\.\d+)?배|저장 상위|상위 \d/)이 등장하지 않는다. (입력 signatureMenu에 숫자가 없는 한.)
    - Test 5: 5개 톤 각각 호출 시 같은 실주제(동일 signatureMenu)라도 slideLabel/문체가 톤별로 다르다 (editorial=serif 한글, minimal=짧은 사실, premium=영문 라벨 등).
  </behavior>
  <action>
실주제 → 톤별 문체 래퍼 구조로 재설계. 핵심: **주제(subject)를 데이터에서 도출하고, 톤은 그 subject를 감싸는 템플릿**이 되게 한다. 주제 하드코딩 회귀 절대 금지.

1) `getShopHand` 시그니처를 하위호환 확장 (옵셔널 인자 추가 — 기존 데모 호출 무손상):
   `export function getShopHand(brand: Brand, tone: VoiceTone, now = new Date(), realData?: { signatureMenu?: string[]; tagline?: string }): ShopHand`

2) 주제 도출 순수함수 `resolveSubject(brand, realData, now): { subject: string; source: "menu" | "campaign" | "season"; topic?: Topic }` 신설:
   - 우선순위 ① `realData.signatureMenu?.[0]` (실메뉴) → source="menu"
   - ② `brand.campaign` 이 placeholder("첫 캠페인" 등 무드태그 자동생성)가 아니면 사용 → source="campaign"
   - ③ `getSeasonalTopics(brand.industry, undefined, now)` 에서 `deterministicPick(topics, \`${brand.id}-${tone}-${datestamp}\`)` 으로 1개 → source="season", topic 보관
   - 여러 시그니처 메뉴가 있으면 deterministicPick으로 그날의 1개 선택(결정론 시드에 datestamp 포함하여 일단위 로테이션).

3) `copyPool`(editorialPool/minimalPool/warmShopPool/wittyPool/premiumPool)을 **subject를 받는 톤별 템플릿 함수**로 재작성:
   - 시그니처: `editorialTemplate(brand, ctx, subject, source): CopyPool` 형태. 하드코딩 "에티오피아"/"60개"/"휘낭시에" 등 **고정 주제 문자열 제거** — 전부 `subject` 변수로 대체.
   - 각 톤의 **문체·라벨·titleFont·tone(무드)** 결정 로직은 보존(톤 정체성). 예: editorial=짧고 거리감+serif, minimal=사실만+sans+영문 라벨, warm-shop=구어체, witty=한 줄 농담, premium=영문 라벨+serif.
   - slideHint/caption은 source에 따라 분기: menu→"오늘 골라둔 한 컷"류 실메뉴 안내(수량/시간 단정 금지), season→topic.intent를 자연스럽게 녹임.
   - **가짜수치 금지**: "60개"·"+2.4배"·"저장 상위"·구체 잔여수량 신규 생성 금지. signatureMenu에 사용자가 직접 넣은 숫자는 그대로 통과 OK.
   - **카피 클리셰 금지**(CLAUDE.md): 팔로우+알림ON, DM 영문 트리거, 저장각/저장 필수, FOMO "놓치지 마세요". 사장님 구어체 유지.
   - **한글 가짜 이탤릭 금지**: 카피 문자열에 italic 지시 없음(렌더는 기존 Nanum Myeongjo, 변경 안 함).

4) `slideLabel`은 톤별 영문/한글 라벨 유지하되 고정 주제 의존 제거. season source일 때 `getSeasonContext(now).monthLabel` 활용 가능.

5) `buildGreeting`/`buildWeek`/`buildReactions`/`TONE_*`/`moodGradient`/`pickPublishTime`는 **범위 밖 — 변경 최소화**(보존). reactions의 기존 가짜수치는 이번 범위 아님(today post 주제만). 단 새 가짜수치 추가 금지.

6) `TodayPost` 타입은 **형태 불변**(slideLabel/slideTitle/slideHint/caption/hashtags/reasoning/cover/...) — 소비처 무손상.

7) `reasoning`(Editor's Note)에 도출 출처를 정직하게: menu="시그니처 메뉴 기반", season="현재 시즌 추천 주제" 등 1줄.

파일 상단 주석("직원 보고용 가짜 데이터")을 today post가 실데이터 기반임을 반영하도록 갱신.
  </action>
  <verify>
    <automated>cd briq-app && ./node_modules/.bin/vitest run lib/dummy/today-shift.test.ts</automated>
  </verify>
  <done>5개 톤 모두 signatureMenu 주입 시 실메뉴를 주제로 출력, 폴백은 시즌/캠페인 주제, deterministicPick 결정론 유지, 신규 가짜수치 0, vitest 그린. tsc --noEmit 0.</done>
</task>

<task type="auto">
  <name>Task 2: Dashboard.tsx — 활성 브랜드 실데이터 전달 + 정직한 라벨 정정</name>
  <files>briq-app/components/dashboard/Dashboard.tsx</files>
  <action>
1) `useBrand()` 에서 `userBrands` 를 추가로 구조분해(`const { brand, userBrands } = useBrand()`).

2) **활성 브랜드의 실데이터 조회** (멀티브랜드 정합 — 단일 userBrand 사용 금지):
   `const activeUser = userBrands.find((b) => b.id === brand.id) ?? null;`
   (BrandProvider에서 활성 brand가 userBrands.find(b => b.id === brandId)로 파생되므로 이 매칭이 정답.)

3) `getShopHand` 호출부(~line 132)에 실데이터 전달:
   `setShop(getShopHand(brand, tone, new Date(), { signatureMenu: activeUser?.signatureMenu, tagline: activeUser?.tagline }));`
   useEffect deps에 activeUser(또는 brand.id) 반영.

4) **정직한 라벨**(CLAUDE.md 정직성 #7): FEATURE/AutomationStatus 영역에서 더미를 실데이터로 오인시키는 표기를 출처에 맞게 조정.
   - signatureMenu/실campaign 입력이 있는 브랜드 → 실데이터 기반이므로 현 라벨 유지 가능.
   - 시즌/폴백 추정(activeUser 없음 또는 signatureMenu 없음) → "예시" 성격을 명시(예: FEATURE 영역 또는 Editor's Note 근처에 "시즌 추천 기반 예시" 류 캡션). 디자인 토큰 준수(INK_MUTE 캡션, 새 색 금지, SAGE 외 액센트 금지, 이모지 금지, rounded-xl 금지). 한글 캡션은 가짜 이탤릭 금지·tracking 0.01~0.02em.
   - 정직성 위반인 단정형 문구(예: "AUTO OPERATIONS · LIVE"가 추정 데이터를 실시간 운영으로 오인시키는 경우)를 추정 출처일 때 톤다운.

5) InstagramMobilePreview/CoverStory에 넘기는 `shop` 형태는 불변 — props 변경 없음.

범위 엄수: today post 주제·라벨 정직성만. week 캘린더·reactions·greeting·CoverStory 이미지 로직은 건드리지 않는다.
  </action>
  <verify>
    <automated>cd briq-app && ./node_modules/.bin/tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <done>활성 브랜드 실데이터(signatureMenu/tagline)가 getShopHand로 전달되고, 데이터 출처에 따라 라벨이 정직하게 표기됨. tsc 0 에러. InstagramMobilePreview 무손상.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>today post 주제를 브랜드 실데이터(시그니처 메뉴 → 캠페인 → 현시즌 주제)로 전환하고, 5개 톤을 실주제 래퍼로 재구성, FEATURE 카드 라벨을 출처에 맞게 정직하게 정정함.</what-built>
  <how-to-verify>
1) `cd briq-app && pnpm dev` (또는 기존 dev 서버) → http://localhost:3000 대시보드 접속.
2) 온보딩으로 signatureMenu를 가진 브랜드 선택 → FEATURE/today post의 slideTitle·caption에 **실제 시그니처 메뉴**가 반영되는지 확인. 톤 토글(에디토리얼/미니멀/친근/위트/프리미엄) 전환 시 **같은 메뉴 주제가 톤별 문체로** 바뀌는지(주제 자체가 "60개"로 고정되지 않는지) 확인.
3) signatureMenu 없는 데모 브랜드 선택 → 주제가 **현시즌 추천 주제**(예: 6월=여름)에서 나오고, "예시/시즌 추천" 성격 라벨이 보이는지 확인.
4) 헤드리스 스크린샷 비교(전/후): `./node_modules/.bin/<chrome> --headless=new --screenshot` 또는 CDP. 디자인 토큰(INK/SAGE/RULE/PAPER) 일관, 가짜 이탤릭/이모지/새 색 없음, 가짜수치 미생성 육안 확인.
5) InstagramMobilePreview 슬라이드(표지/본문)가 깨지지 않고 같은 주제로 렌더되는지 확인.
  </how-to-verify>
  <resume-signal>"approved" 입력 또는 문제점 기술</resume-signal>
</task>

</tasks>

<verification>
- `cd briq-app && ./node_modules/.bin/tsc --noEmit -p tsconfig.json` → 0 에러.
- `cd briq-app && ./node_modules/.bin/vitest run lib/dummy/today-shift.test.ts` → 그린.
- 헤드리스 스크린샷 전/후 육안: 실데이터 주제 반영 + 디자인 토큰 일관 + 정직성 라벨.
</verification>

<success_criteria>
- today post 주제가 하드코딩 풀에서 분리되어 실데이터(메뉴→캠페인→시즌)에서 도출됨.
- 5개 톤이 같은 실주제를 톤별 문체로 감싸 출력(주제 하드코딩 회귀 없음, deterministicPick 결정론 유지).
- 신규 가짜수치 0, 카피 클리셰 0, 한글 가짜 이탤릭 0.
- FEATURE 라벨이 데이터 출처에 따라 정직하게 표기(CLAUDE.md #7).
- InstagramMobilePreview·CoverStory 무손상(TodayPost 형태 불변).
</success_criteria>

<output>
After completion, create `.planning/quick/260608-ny3-today-post/260608-ny3-SUMMARY.md`
</output>
