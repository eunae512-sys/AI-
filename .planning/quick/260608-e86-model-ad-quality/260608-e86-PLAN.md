---
phase: quick-260608-e86
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - briq-app/lib/ai-gen/model-scenes.ts
  - briq-app/lib/api/demo-images.ts
  - briq-app/app/api/generate-image/route.ts
  - briq-app/components/ai-gen/AiModelGenerator.tsx
autonomous: true
requirements: [E86-AD-QUALITY, E86-SCENE-AWARE-FALLBACK]

must_haves:
  truths:
    - "Imagen 4 경로(source=gemini-imagen4)는 여전히 동작하며 광고급 톤이 강화됨"
    - "폴백(Pexels)이 씬을 인식 — closeup/overhead/product 씬은 사물/공간 사진, portrait/lifestyle 씬은 한국인+씬 배경"
    - "48개 ModelScene 전부 fallbackQuery 보유, 비어있지 않음"
    - "기존 fetchPortraitFromPexels/portraitQueryForIndustry 시그니처 무손상 (하위호환)"
  artifacts:
    - path: "briq-app/lib/ai-gen/model-scenes.ts"
      provides: "COMMON_TAIL 광고급 보강 + ModelScene.fallbackQuery (48개)"
      contains: "fallbackQuery"
    - path: "briq-app/lib/api/demo-images.ts"
      provides: "fetchSceneFromPexels({query, allowPeople, seed})"
      contains: "fetchSceneFromPexels"
    - path: "briq-app/app/api/generate-image/route.ts"
      provides: "gracefulFallback 가 fallbackQuery+frame 인식"
      contains: "fetchSceneFromPexels"
    - path: "briq-app/components/ai-gen/AiModelGenerator.tsx"
      provides: "generate() body 에 fallbackQuery+frame 전달"
      contains: "fallbackQuery"
  key_links:
    - from: "AiModelGenerator.generate()"
      to: "/api/generate-image"
      via: "body { fallbackQuery: scene.fallbackQuery, frame: scene.frame }"
      pattern: "fallbackQuery:\\s*scene\\.fallbackQuery"
    - from: "route.ts gracefulFallback"
      to: "fetchSceneFromPexels"
      via: "fallbackQuery 있으면 씬 검색, allowPeople = frame in [portrait,lifestyle]"
      pattern: "fetchSceneFromPexels"
---

<objective>
AI 출연자(모델 씬) 광고 품질 + 주제 일치 개선. 두 갈래:
1. **Imagen 프롬프트 광고급 강화** — COMMON_TAIL 에 커머셜 광고 사진 디렉티브 1~2구 추가(정상 Imagen 경로 무손상).
2. **폴백 씬 인식 (핵심)** — 현재 쿼터/빌링/프로바이더 부재 시 gracefulFallback 이 씬을 무시하고 항상 generic 인물(fetchPortraitFromPexels)을 반환 → "객실 준비"인데 카페 여성 같은 주제 무관 저품질 사진. ModelScene 에 씬별 영문 fallbackQuery 추가 + fetchSceneFromPexels(query 직접 받음, 인물 씬 아니면 사람 페널티) 추가 + route/컴포넌트 배선.

Purpose: 사장님이 보는 출연자 사진이 Imagen 실패 시에도 주제·씬과 맞는 광고급 사진이 되도록.
Output: model-scenes.ts(COMMON_TAIL+48 fallbackQuery), demo-images.ts(fetchSceneFromPexels), route.ts·AiModelGenerator.tsx 배선.

제약: Imagen(정상) 경로 무손상. 과금·광고법 라벨(AI 생성)·로직 무손상. fetchPortraitFromPexels/portraitQueryForIndustry 시그니처 깨지 말 것(신규 함수 추가). fallbackQuery 영문·한국+씬·가짜 텍스트 없음.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@briq-app/lib/ai-gen/model-scenes.ts
@briq-app/lib/api/demo-images.ts
@briq-app/app/api/generate-image/route.ts
@briq-app/components/ai-gen/AiModelGenerator.tsx

<interfaces>
<!-- 실행자가 바로 쓸 핵심 계약. 코드 탐색 불필요. -->

ModelScene 타입 (model-scenes.ts @20-31) — `frame: FrameStyle` 보유:
  FrameStyle = "portrait" | "lifestyle" | "overhead" | "closeup"
  portrait·lifestyle = 인물 있음 / overhead·closeup·(role==="product") = 사물·공간

기존 폴백 (route.ts @57) — 무손상 유지, 하위호환 백업으로 남김:
  fetchPortraitFromPexels({ industry, gender, seed }): Promise<DemoImage | null>
  portraitQueryForIndustry({ industry, gender }): string

search-pexels route 의 인물 페널티 패턴 (참고, app/api/search-pexels/route.ts @161-217):
  - alt 텍스트에 person/people 키워드 매칭 시 score -= 50 * hits
  - allowPeople=false 면 filtered = scored.filter(s => s.score > -20)
  - 인물 키워드 예: "person standing","person sitting","people walking","young woman","young man","businessman","businesswoman","woman wearing","man wearing"

DemoImage 타입 (demo-images.ts @5-13): { url, keywords, industry?, photographer, photographerUrl, pexelsUrl, alt }

generate-image route body 가 이미 받는 필드: prompt, size, quality, slideId, industry, gender, personaSeed, engine
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: COMMON_TAIL 광고급 보강 + 48개 ModelScene 에 fallbackQuery 추가</name>
  <files>briq-app/lib/ai-gen/model-scenes.ts</files>
  <action>
**(A) COMMON_TAIL 보강 (@52-58):**
기존 라인(익명성·Korean only·no text/logo) 전부 유지하고, 맨 앞 또는 "Authentic, unstaged feel" 라인 근처에 커머셜 광고 디렉티브 1~2구 통합. "unstaged/not stock photo"(자연스러움)와 "commercial advertising"(연출)이 모순돼 보이지 않게 — "editorial commercial" 톤으로 묶는다. 추가 문구 예:
  "Professional commercial advertising photography, high detail, sharp focus, polished editorial lighting, premium magazine quality. "
→ COMMON_TAIL 시작 부분에 위 1구 삽입(기존 "Natural editorial photography..." 바로 앞). 과하게 늘리지 말 것(1~2 문장).
genderDescriptor/ageDescriptor/각 씬 promptEN 디테일은 **손대지 말 것** — COMMON_TAIL만.

**(B) ModelScene 타입에 fallbackQuery 추가 (@20-31):**
  `fallbackQuery: string;` 를 frame 다음에 required 필드로 추가. (선택 아님 — 48개 전부 작성하므로 required 로 두면 누락 시 tsc 가 잡아줌.)

**(C) 48개 씬 전부 fallbackQuery 작성.** 규칙:
  - frame in [closeup, overhead] 또는 role==="product" → **사물/공간 검색어. 사람 단어(woman/man/person/host/guest/customer/owner/staff/barista/chef/stylist) 절대 금지.** "korean" + 씬 사물/공간.
  - frame in [portrait, lifestyle] (단 role==="product" 제외) → "korean " + 사람 + 씬 배경. (예외: stay-room-empty 는 frame=lifestyle 이지만 role=product → 사물 검색.)
  영문, 한국+씬, 가짜 텍스트/로고 단어 없음, 소문자 키워드 나열형.

전체 48개 fallbackQuery (id → query):

음식점:
  rest-chef-plating (closeup) → "korean dish plating chef hands garnish wooden counter close up"
  rest-owner-greeting (portrait) → "korean restaurant owner woman apron entrance warm interior"
  rest-customer-eating (lifestyle) → "korean person eating korean food restaurant table chopsticks candid"
  rest-overhead-table (overhead) → "korean restaurant table overhead flat lay side dishes wooden"
  rest-owner-cooking (lifestyle) → "korean chef cooking wok flame stove kitchen steam"
  rest-staff-serving (closeup) → "korean dish served hands plate wooden table steam close up"
  rest-customer-cheers (lifestyle) → "korean people toast glasses restaurant table evening candid"
  rest-product-closeup (closeup, product) → "korean dish ceramic bowl steam close up dark wood still life"

카페:
  cafe-barista-latte (closeup) → "latte art pouring hands ceramic cup cafe counter close up"
  cafe-owner-counter (portrait) → "korean cafe owner woman counter coffee warm interior"
  cafe-customer-reading (lifestyle) → "korean person cafe window coffee book afternoon candid"
  cafe-overhead-drink (overhead) → "latte dessert marble cafe table overhead flat lay still life"
  cafe-owner-roasting (closeup) → "roasted coffee beans hands cupping roastery close up"
  cafe-staff-handoff (closeup) → "coffee cup handed across wooden cafe counter close up"
  cafe-customer-laptop (lifestyle) → "korean person laptop cafe table coffee window candid"
  cafe-product-pour (closeup, product) → "iced coffee pouring glass ice splash marble still life no people"

디저트:
  dessert-piping (closeup) → "pastry cream piping hands dessert marble berries close up"
  dessert-owner-tray (portrait) → "korean dessert shop owner woman tray pastries pastel interior"
  dessert-customer-bite (lifestyle) → "korean person eating dessert cafe soft light candid"
  dessert-overhead-plate (overhead, product) → "artisan dessert ceramic plate marble overhead flat lay still life"
  dessert-staff-decorate (closeup) → "pastry decorating berries tweezers cake marble close up"
  dessert-owner-counter (portrait) → "korean dessert shop owner woman pastry showcase pastel interior"
  dessert-customer-share (lifestyle) → "korean people sharing dessert two forks plate cafe candid"
  dessert-product-stack (closeup, product) → "layered cake slice cross section ceramic plate marble still life"

숙박/스테이:
  stay-host-welcome (portrait) → "korean hanok host woman wooden veranda linen welcoming warm light"
  stay-staff-prep (closeup) → "korean hanok room folded white linen bedding tea tray morning light"
  stay-guest-tea (lifestyle) → "korean person hanok wooden veranda tea cup garden calm"
  stay-room-empty (lifestyle, product) → "empty korean hanok interior sunlight wooden lattice futon tea set"
  stay-host-checkin (closeup) → "brass key wooden tray hanok entrance hands close up"
  stay-staff-tea (closeup) → "korean tea cup sweets wooden tray hanok floor hands close up"
  stay-guest-window (lifestyle) → "korean person hanok paper window tea cup linen morning calm"
  stay-product-detail (closeup, product) → "korean tea table hanok still life ceramic tea set lattice shadow"

미용/뷰티:
  beauty-stylist-work (closeup) → "hair stylist hands shears cutting salon close up"
  beauty-owner-portrait (portrait) → "korean hair salon director woman black apron minimalist interior"
  beauty-customer-mirror (lifestyle) → "korean person salon mirror styled hair soft light candid"
  beauty-station-tools (overhead, product) → "hair styling tools brush scissors comb marble counter overhead flat lay"
  beauty-staff-color (closeup) → "hair colorist gloved hands color brush foils salon close up"
  beauty-staff-blowdry (closeup) → "hair blow dry round brush dryer salon hands close up"
  beauty-owner-consult (lifestyle) → "korean hair salon director woman mirror consulting minimalist interior"
  beauty-customer-relax (closeup) → "korean person shampoo basin eyes closed relaxed salon close up"
  beauty-product-shelf (overhead, product) → "hair care products bottles wooden comb marble counter overhead flat lay"

로컬/패션/잡화:
  local-owner-store (portrait) → "korean boutique owner woman clothing rack fashion store linen apron"
  local-customer-fitting (lifestyle) → "korean person touching fabric garment boutique afternoon candid"
  local-product-flatlay (overhead, product) → "folded linen garment leather accessory dried flowers neutral overhead flat lay"
  local-owner-wrapping (closeup) → "hands wrapping item kraft paper twine boutique counter close up"
  local-owner-display (lifestyle) → "korean boutique owner woman arranging shelf lifestyle store warm light"
  local-customer-browse (closeup) → "hands lifting handcrafted item boutique shelves close up"
  local-product-detail (closeup, product) → "handcrafted product material texture linen surface dried flower still life"

**검증 자가체크:** closeup/overhead/product 8+개 group 의 query 에 woman/man/person/host/guest/customer/owner/staff/barista/chef/stylist 단어가 없는지 눈으로 확인. portrait/lifestyle(비-product) query 에 "korean" + 사람단어 포함 확인.
  </action>
  <verify>
    <automated>cd briq-app && ./node_modules/.bin/tsc --noEmit -p tsconfig.json</automated>
    <automated>cd briq-app && node -e "const{MODEL_SCENES}=require('jiti')(process.cwd())('./lib/ai-gen/model-scenes.ts');const PEOPLE=/\b(woman|man|person|host|guest|customer|owner|staff|barista|chef|stylist|colorist|director)\b/;let bad=[];for(const s of MODEL_SCENES){if(!s.fallbackQuery||!s.fallbackQuery.trim())bad.push(s.id+':empty');const noPeople=s.frame==='closeup'||s.frame==='overhead'||s.role==='product';if(noPeople&&PEOPLE.test(s.fallbackQuery))bad.push(s.id+':has-people:'+s.fallbackQuery);const wantPeople=(s.frame==='portrait'||s.frame==='lifestyle')&&s.role!=='product';if(wantPeople&&!/korean/.test(s.fallbackQuery))bad.push(s.id+':no-korean');}console.log('count',MODEL_SCENES.length);if(bad.length){console.error('FAIL',bad);process.exit(1)}console.log('OK all fallbackQuery valid')"</automated>
    <automated>cd briq-app && node -e "const fs=require('fs');const t=fs.readFileSync('lib/ai-gen/model-scenes.ts','utf8');if(!/commercial advertising/i.test(t)){console.error('COMMON_TAIL missing ad directive');process.exit(1)}console.log('OK ad directive present')"</automated>
  </verify>
  <done>tsc 0 에러. 48개 fallbackQuery 전부 존재·비어있지 않음. 사물 씬(closeup/overhead/product)에 인물 단어 부재, 인물 씬(portrait/lifestyle non-product)에 korean 포함. COMMON_TAIL 에 "commercial advertising" 구문 존재. genderDescriptor/promptEN 미변경.</done>
</task>

<task type="auto">
  <name>Task 2: demo-images.ts 에 fetchSceneFromPexels 추가 (인물 페널티 포함)</name>
  <files>briq-app/lib/api/demo-images.ts</files>
  <action>
신규 export 함수 추가 (기존 fetchPortraitFromPexels/portraitQueryForIndustry **수정 금지** — 하위호환). search-pexels route 의 인물 페널티 스코어링 패턴(@161-217)을 참고해 단순화 구현.

```ts
/**
 * 씬 인식 폴백 — 호출 측이 query 를 직접 지정(ModelScene.fallbackQuery).
 * allowPeople=false 면 alt 텍스트에 인물 키워드가 있는 결과를 페널티/제외 →
 * 손·사물·공간 씬에 generic 인물이 떨어지는 문제 방지.
 * API 키 없으면 null → 호출 측이 다른 폴백으로 처리.
 */
export async function fetchSceneFromPexels(opts: {
  query: string;
  allowPeople: boolean;
  seed?: string;
}): Promise<DemoImage | null> {
  const apiKey = process.env.PEXELS_API_KEY ?? "";
  if (!apiKey || apiKey.trim() === "" || apiKey === "YOUR_PEXELS_KEY") return null;
  const q = (opts.query ?? "").trim();
  if (!q) return null;

  const params = new URLSearchParams({
    query: q,
    orientation: "portrait",
    size: "large",
    per_page: "24",
    locale: "en-US",
  });
  try {
    const r = await fetch(`https://api.pexels.com/v1/search?${params.toString()}`, {
      headers: { Authorization: apiKey },
      signal: AbortSignal.timeout(8000),
    });
    if (!r.ok) return null;
    const data = (await r.json()) as {
      photos?: Array<{
        id: number; url: string; alt: string | null;
        photographer: string; photographer_url: string;
        src: { large2x?: string; large?: string; original?: string };
      }>;
    };
    const photos = data.photos ?? [];
    if (photos.length === 0) return null;

    // 인물 키워드 페널티 (allowPeople=false 일 때만). search-pexels 패턴 차용.
    const PEOPLE_KW = [
      "person standing","person sitting","people walking","young woman","young man",
      "woman wearing","man wearing","businessman","businesswoman","portrait of",
      "smiling woman","smiling man","selfie","model posing",
    ];
    const scored = photos.map((p, i) => {
      let score = Math.max(0, 5 - i * 0.2); // 앞쪽 결과 가산(연관도)
      if (!opts.allowPeople) {
        const alt = (p.alt ?? "").toLowerCase();
        let hits = 0;
        for (const k of PEOPLE_KW) if (alt.includes(k)) hits++;
        if (hits > 0) score -= 50 * hits;
      }
      return { p, i, score };
    });
    scored.sort((a, b) => b.score - a.score);
    const filtered = opts.allowPeople ? scored : scored.filter((s) => s.score > -20);
    const pool = filtered.length > 0 ? filtered : scored;

    // seed 로 결정적 픽 — pool 상위권 안에서 선택(연관도 유지)
    const seedStr = String(opts.seed ?? "");
    let h = 0;
    for (let i = 0; i < seedStr.length; i++) h = (h * 31 + seedStr.charCodeAt(i)) | 0;
    const topN = Math.min(pool.length, 8); // 연관도 높은 상위에서만 변주
    const pick = pool[Math.abs(h) % topN].p;

    return {
      url: pick.src.large2x || pick.src.large || pick.src.original || "",
      keywords: ["scene-fallback"],
      photographer: pick.photographer,
      photographerUrl: pick.photographer_url,
      pexelsUrl: pick.url,
      alt: pick.alt || q,
    };
  } catch {
    return null;
  }
}
```

배치: fetchPortraitFromPexels 함수 바로 다음(@180 이후)에 추가. import 추가 불필요(같은 파일 내 DemoImage 타입 재사용).
  </action>
  <verify>
    <automated>cd briq-app && ./node_modules/.bin/tsc --noEmit -p tsconfig.json</automated>
    <automated>cd briq-app && node -e "const m=require('jiti')(process.cwd())('./lib/api/demo-images.ts');if(typeof m.fetchSceneFromPexels!=='function'){console.error('fetchSceneFromPexels not exported');process.exit(1)}if(typeof m.fetchPortraitFromPexels!=='function'||typeof m.portraitQueryForIndustry!=='function'){console.error('legacy fns broken');process.exit(1)}console.log('OK exports intact')"</automated>
  </verify>
  <done>tsc 0. fetchSceneFromPexels export 됨. 기존 fetchPortraitFromPexels/portraitQueryForIndustry export 무손상. PEXELS_API_KEY 없으면 null 반환 가드 동일.</done>
</task>

<task type="auto">
  <name>Task 3: route.ts gracefulFallback 씬 인식 배선 + AiModelGenerator 전달</name>
  <files>briq-app/app/api/generate-image/route.ts, briq-app/components/ai-gen/AiModelGenerator.tsx</files>
  <action>
**(A) route.ts:**
1. import 에 `fetchSceneFromPexels` 추가 (@2-5 의 demo-images import 블록).
2. body 파싱부(@30-40 근처)에 추가:
   ```ts
   const fallbackQuery = typeof body.fallbackQuery === "string" && body.fallbackQuery.trim() ? body.fallbackQuery.trim() : undefined;
   const frame = typeof body.frame === "string" ? body.frame : undefined;
   const allowPeople = frame === "portrait" || frame === "lifestyle";
   ```
3. gracefulFallback 내부(@56-57) 의 portrait 페치를 분기:
   ```ts
   const portrait = fallbackQuery
     ? await fetchSceneFromPexels({ query: fallbackQuery, allowPeople, seed: personaSeed })
     : await fetchPortraitFromPexels({ industry, gender, seed: personaSeed });
   ```
   나머지(if(portrait){...} 응답, demo 풀 최종 폴백)는 그대로 유지. meta.source 는 기존 "pexels-portrait-fallback" 유지(과금/로직 무손상; 굳이 새 source 라벨 안 만들어도 됨 — 선택적으로 fallbackQuery 있을 때 "pexels-scene-fallback" 로 표기해도 무방하나 필수 아님).
   → 권장: 구분 위해 source 만 분기 `source: fallbackQuery ? "pexels-scene-fallback" : "pexels-portrait-fallback"`.

**Imagen 정상 경로(@149-215) 절대 미변경.** 과금(ensurePlanAndQuota/bumpUsage)·광고법 fortifiedPrompt 무손상.

**(B) AiModelGenerator.tsx generate() body (@105-115):**
JSON body 에 두 필드 추가:
   ```ts
   fallbackQuery: scene.fallbackQuery,
   frame: scene.frame,
   ```
   (industry/gender/personaSeed 기존 유지.) scene 은 이미 ModelScene 객체라 .fallbackQuery/.frame 접근 가능.
  </action>
  <verify>
    <automated>cd briq-app && ./node_modules/.bin/tsc --noEmit -p tsconfig.json</automated>
    <automated>cd briq-app && node -e "const fs=require('fs');const r=fs.readFileSync('app/api/generate-image/route.ts','utf8');const c=fs.readFileSync('components/ai-gen/AiModelGenerator.tsx','utf8');const fail=[];if(!/fetchSceneFromPexels/.test(r))fail.push('route missing fetchSceneFromPexels');if(!/fallbackQuery/.test(r))fail.push('route missing fallbackQuery');if(!/allowPeople/.test(r))fail.push('route missing allowPeople');if(!/fallbackQuery:\s*scene\.fallbackQuery/.test(c))fail.push('component missing fallbackQuery pass');if(!/frame:\s*scene\.frame/.test(c))fail.push('component missing frame pass');if(!/ensurePlanAndQuota/.test(r))fail.push('route lost billing gate');if(!/fortifiedPrompt/.test(r))fail.push('route lost compliance prompt');if(fail.length){console.error('FAIL',fail);process.exit(1)}console.log('OK wiring intact')"</automated>
  </verify>
  <done>tsc 0. route 가 fallbackQuery 있으면 fetchSceneFromPexels(allowPeople=frame∈[portrait,lifestyle]), 없으면 기존 portrait 폴백(하위호환). 최종 demo 풀 폴백 유지. 과금 게이트·fortifiedPrompt 무손상. 컴포넌트가 fallbackQuery+frame 전달. Imagen 경로 미변경.</done>
</task>

</tasks>

<verification>
1. `cd briq-app && ./node_modules/.bin/tsc --noEmit -p tsconfig.json` → 0.
2. jiti 프로브 3종(Task1/2/3 의 automated) 통과.
3. (실호출, dev:3000 가능 시)
   (a) 정상 — 모델 씬 1장 생성, 응답 meta.source 가 gemini-imagen4(또는 설정된 프로바이더)인지 = Imagen 경로 무손상 확인.
   (b) 폴백 단위 — fetchSceneFromPexels({query:"korean hanok room folded white linen bedding tea tray morning light", allowPeople:false}) 직접 호출(스크립트) → 반환 alt 가 인물 아닌 객실/이불/공간류인지 육안. (PEXELS_API_KEY 없으면 null → 스킵.)
4. (선택) 헤드리스 스크린샷으로 출력 톤 육안.
5. 워크트리 미사용 — 현재 브랜치에서 작업.
</verification>

<success_criteria>
- COMMON_TAIL 에 커머셜 광고 디렉티브 통합(기존 익명성·Korean·no-text 라인 유지, 모순 없음).
- 48개 ModelScene 전부 fallbackQuery — 사물 씬은 사물 검색(인물 단어 0), 인물 씬은 korean+사람+배경.
- fetchSceneFromPexels 추가, 인물 씬 아니면 person 페널티. 기존 함수 시그니처 무손상.
- route gracefulFallback 이 씬 인식(fallbackQuery+frame), 없으면 하위호환 portrait 폴백. demo 풀 최종 폴백 유지.
- AiModelGenerator 가 fallbackQuery+frame 전달.
- Imagen 정상 경로·과금·광고법 라벨 전부 무손상. tsc 0.
</success_criteria>

<output>
After completion, create `.planning/quick/260608-e86-model-ad-quality/260608-e86-SUMMARY.md`
</output>
