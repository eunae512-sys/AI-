---
phase: quick-260608-0ux
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - briq-app/lib/brand/user-brand.ts
  - briq-app/components/brand/BrandProvider.tsx
  - briq-app/components/layout/BrandSwitcher.tsx
  - briq-app/components/layout/Sidebar.tsx
  - briq-app/lib/nav.ts
  - briq-app/components/onboarding/Onboarding.tsx
autonomous: false
requirements: [UX-AGENCY-ADAPTIVE-NAV]
must_haves:
  truths:
    - "다중 사용자 브랜드가 localStorage 에 영속화되고, 단일키만 있던 기존 사용자는 1회 자동 마이그레이션된다"
    - "isAgency 는 실사용자 브랜드 수(userBrands.length) >= 2 일 때만 true 다 (더미 제외)"
    - "Sidebar 에서 브랜드가 2개 이상이면 드롭다운 스위처로 active 브랜드를 전환할 수 있다"
    - "솔로(실브랜드 1)면 드롭다운 없는 단일 브랜드명만 보이고 클라이언트 메뉴가 숨겨진다"
    - "대행사(실브랜드 2+) 또는 데모(실브랜드 0→더미)면 클라이언트 메뉴가 보인다"
    - "온보딩에 ?add=1 로 진입하면 기존 브랜드를 덮어쓰지 않고 새 브랜드를 목록에 추가·활성화한다"
  artifacts:
    - path: "briq-app/lib/brand/user-brand.ts"
      provides: "loadUserBrands / addUserBrand / removeUserBrand / saveUserBrands + 단일→다중 마이그레이션"
      contains: "loadUserBrands"
    - path: "briq-app/components/brand/BrandProvider.tsx"
      provides: "userBrands / isAgency / addBrand 노출, allBrands 재정의"
      contains: "isAgency"
    - path: "briq-app/components/layout/BrandSwitcher.tsx"
      provides: "에디토리얼 토큰 기반 적응형 브랜드 스위처"
      min_lines: 60
    - path: "briq-app/lib/nav.ts"
      provides: "NavItem.agencyOnly 플래그 + 클라이언트 항목 노출"
      contains: "agencyOnly"
  key_links:
    - from: "briq-app/components/layout/Sidebar.tsx"
      to: "briq-app/components/layout/BrandSwitcher.tsx"
      via: "유저 플레이트 영역에서 렌더"
      pattern: "BrandSwitcher"
    - from: "briq-app/components/layout/Sidebar.tsx"
      to: "useBrand().isAgency"
      via: "navGroups 필터에서 agencyOnly 항목 게이팅"
      pattern: "agencyOnly"
    - from: "briq-app/components/onboarding/Onboarding.tsx"
      to: "useBrand().addBrand"
      via: "?add=1 분기에서 addBrand 호출"
      pattern: "addBrand"
---

<objective>
적응형 nav 의 대행사 레이어를 구현한다: 다중 브랜드 저장 모델 + 브랜드 스위처 UI + 실브랜드 수 기반 적응형 nav(클라이언트 메뉴 게이팅) + 온보딩 append 모드.

Purpose: 단일 페르소나(솔로 사장님) 가정으로 묶여 있던 데이터/네비 구조를 dual-persona(솔로 + 대행사) 로 확장. 브랜드를 여러 개 가진 대행사 사용자는 자기 브랜드 사이를 전환하고 클라이언트 관리에 진입할 수 있어야 한다.

Output: 다중 브랜드 영속화 API, 확장된 BrandProvider, 신규 BrandSwitcher 컴포넌트, agencyOnly nav 게이팅, 온보딩 append 분기.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@../../../CLAUDE.md

<interfaces>
<!-- 실행자가 그대로 쓸 현재 계약. 코드베이스 재탐색 불필요. -->

briq-app/lib/brand/user-brand.ts (현재):
- export type UserBrandData = { id; name; industry; industryLabel; city; moodId; moodTag; palette; photos; hook; toneText; tagline?; signatureMenu?; ownerName?; ownerEmail?; selectedPlan?; createdAt }
- const STORAGE_KEY = "briq:user-brand";  // 단일
- saveUserBrand(data): void   // QuotaExceeded 시 photos.slice(0,3) 재시도
- loadUserBrand(): UserBrandData | null  // 유효성: id && industry && Array.isArray(palette)
- clearUserBrand(): void
- toBrand(u: UserBrandData): Brand

briq-app/components/brand/BrandProvider.tsx (현재):
- Ctx = { brand; brandId; setBrandId(id); allBrands: Brand[]; userBrand: UserBrandData|null; refreshUserBrand() }
- const STORAGE_KEY = "briq-active-brand";  // active 키 (재사용)
- import { brands, getBrand } from "@/lib/dummy/brands"  // brands = 더미 7개, 각 isDemo:true
- 동기화 이벤트: window "briq:user-brand-updated", storage("briq:user-brand")
- allBrands(현재): userBrand 있으면 [toBrand(userBrand), ...brands] 아니면 brands

briq-app/types/index.ts:
- Brand 에 isDemo?: boolean 존재. 더미 7개는 isDemo:true.

briq-app/lib/dummy/brands.ts:
- export const brands: Brand[] (7개, 전부 isDemo:true), getBrand(id): Brand|undefined

briq-app/lib/landing/tokens.ts:
- INK "#14130F" / INK_SOFT "#4A4742" / INK_MUTE "#767268"
- RULE "rgba(20,19,15,0.12)" / RULE_SOFT "rgba(20,19,15,0.06)"
- PAPER "#FAF7EE" / PAPER_HOVER "rgba(20,19,15,0.025)"
- SAGE "#4F5F4B" / HL "#E6DDC8"

briq-app/components/clients/ClientsScreen.tsx (기존, 무손상):
- useBrand() 에서 { setBrandId, allBrands, userBrand } 소비
- hideDemo 토글 + allBrands.filter(b => !b.isDemo) 이미 지원
- "새 브랜드 추가" 버튼이 router.push("/onboarding") 호출 중 → 이 plan 에서 "/onboarding?add=1" 로 변경

briq-app/components/onboarding/Onboarding.tsx (완료 핸들러 @1079-1120):
- import { saveUserBrand } from "@/lib/brand/user-brand"  @10
- const router = useRouter(); const searchParams = useSearchParams();  @232-233
- 완료 onClick @1079: industry 있으면 saveUserBrand({...}) 호출 후 window dispatch "briq:user-brand-updated", 그다음 selectedPlan 분기 router.push
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 다중 브랜드 저장 + 단일→다중 마이그레이션 (user-brand.ts)</name>
  <files>briq-app/lib/brand/user-brand.ts, briq-app/lib/brand/user-brand.test.ts</files>
  <behavior>
    - loadUserBrands(): 새 키 "briq:user-brands" 없고 단일키 "briq:user-brand" 있으면 → [단일] 로 변환·새 키에 1회 저장 후 반환. 둘 다 없으면 [] 반환. 손상 JSON 은 [] (throw 금지).
    - addUserBrand(data): 목록에 같은 id 있으면 교체, 없으면 push. 저장 후 길이 증가.
    - saveUserBrand(data) 보강: 기존 단일키 저장 유지 + 다중 목록에도 upsert(같은 id 교체/없으면 추가). 온보딩 기존 경로 무손상.
    - removeUserBrand(id): 목록에서 제거.
    - 빈 상태: loadUserBrands() === [].
  </behavior>
  <action>
신규 상수: `const BRANDS_KEY = "briq:user-brands";` (기존 `STORAGE_KEY = "briq:user-brand"` 유지).

신규 export 함수 추가:
- `saveUserBrands(list: UserBrandData[]): void` — `localStorage.setItem(BRANDS_KEY, JSON.stringify(list))`. QuotaExceeded catch → 각 brand 의 photos 를 `.slice(0,3)` 으로 슬림화한 list 재시도, 그래도 실패 시 `console.warn`. (기존 saveUserBrand 의 QuotaExceeded 패턴 그대로 차용.)
- `loadUserBrands(): UserBrandData[]` —
  1. `typeof window === "undefined"` → return [].
  2. `localStorage.getItem(BRANDS_KEY)` 있으면 JSON.parse → 배열이면 유효항목만(`b.id && b.industry && Array.isArray(b.palette)`) 필터해 반환. parse 실패 → [].
  3. BRANDS_KEY 없을 때: `loadUserBrand()`(단일) 호출 → 있으면 `[single]` 을 saveUserBrands 로 1회 저장 후 `[single]` 반환. 없으면 [].
- `addUserBrand(data: UserBrandData): void` — `const list = loadUserBrands(); const i = list.findIndex(b => b.id === data.id); if (i >= 0) list[i] = data; else list.push(data); saveUserBrands(list);` 그리고 단일키도 `saveUserBrand` 와 동일하게 active 일관성 위해 갱신할 필요는 없음(active 는 BrandProvider 관리). 단일키 동기화는 saveUserBrand 가 담당.
- `removeUserBrand(id: string): void` — `saveUserBrands(loadUserBrands().filter(b => b.id !== id));`

기존 `saveUserBrand(data)` 보강: 함수 끝(단일키 저장 성공 경로 후)에 다중 목록 upsert 추가 —
```
try {
  const list = loadUserBrands().filter(b => b.id !== data.id);
  list.push(data);
  saveUserBrands(list);
} catch { /* 무시 */ }
```
주의: loadUserBrands 가 내부에서 단일키 마이그레이션을 트리거할 수 있으니, saveUserBrand 는 먼저 단일키를 set 한 뒤 목록 upsert 하도록 순서 유지(이미 단일키 저장이 함수 본문 상단).

테스트 파일 user-brand.test.ts 작성(Vitest, jsdom 또는 localStorage mock):
- describe("loadUserBrands"): (a) 빈 상태 → []. (b) 단일키만 set → [1] 반환 + BRANDS_KEY 에 저장됨(2회 호출해도 1개). (c) BRANDS_KEY 손상 JSON → [].
- describe("addUserBrand"): 빈 상태 add 후 length 1, 같은 id 재add 후에도 length 1(교체), 다른 id add 후 length 2.
- describe("saveUserBrand 보강"): saveUserBrand 호출 후 loadUserBrands 에 해당 id 1개 존재.
localStorage 가 jsdom 에 없으면 테스트 상단에서 간단한 in-memory mock 주입(globalThis.localStorage = {store,getItem,setItem,removeItem}).
  </action>
  <verify>
    <automated>cd briq-app && (./node_modules/.bin/vitest run lib/brand/user-brand.test.ts 2>/dev/null || npx --yes vitest run lib/brand/user-brand.test.ts)</automated>
  </verify>
  <done>user-brand.test.ts 통과: 단일키만 있을 때 [1] 변환, addUserBrand 후 길이 증가/교체, 빈 상태 [] 반환.</done>
</task>

<task type="auto">
  <name>Task 2: BrandProvider 확장 — userBrands / isAgency / addBrand + allBrands 재정의</name>
  <files>briq-app/components/brand/BrandProvider.tsx</files>
  <action>
import 추가: `loadUserBrands, addUserBrand` (from "@/lib/brand/user-brand").

Ctx 타입 확장:
```
userBrands: UserBrandData[];
isAgency: boolean;          // 실사용자 브랜드 2개 이상 (더미 제외)
addBrand: (data: UserBrandData) => void;
```

state 추가: `const [userBrands, setUserBrands] = React.useState<UserBrandData[]>([]);`

refreshUserBrand 재작성: loadUserBrands() 기반.
```
const list = loadUserBrands();
setUserBrands(list);
const primary = list[0] ?? null;
setUserBrand(primary);   // 기존 단일 userBrand 호환 유지 (= 첫 브랜드)
// active 자동 활성화: 저장된 active 가 없거나 더미 기본이고 실브랜드가 있으면 첫 실브랜드로
if (primary && typeof window !== "undefined") {
  const savedActive = localStorage.getItem(STORAGE_KEY);
  if (!savedActive || savedActive === DEFAULT_ID || (!getBrand(savedActive) && !list.some(b => b.id === savedActive))) {
    setBrandIdState(primary.id);
    try { localStorage.setItem(STORAGE_KEY, primary.id); } catch {}
  }
}
```

마운트 effect: 기존처럼 refreshUserBrand 호출 후, saved active 복원 시 `loadUserBrands().some(b => b.id === saved) || getBrand(saved)` 면 setBrandIdState(saved). (단일 loadUserBrand 비교 → 목록 some 비교로 교체.)

storage 동기화 effect: e.key 비교에 `"briq:user-brands"` 도 포함(`if (e.key === "briq:user-brand" || e.key === "briq:user-brands") refreshUserBrand();`). custom event "briq:user-brand-updated" 리스너 유지.

setBrandId 재작성: 사용자 브랜드(목록) 또는 더미에서 찾기.
```
const isUser = userBrands.some(b => b.id === id);
if (!isUser && !getBrand(id)) return;
setBrandIdState(id);
if (typeof window !== "undefined") localStorage.setItem(STORAGE_KEY, id);
```
deps: [userBrands].

allBrands 재정의 (핵심):
```
const allBrands: Brand[] = React.useMemo(() => {
  if (userBrands.length > 0) return userBrands.map(toBrand);  // 실사용자: 자기 브랜드만 (더미 제외)
  return brands;                                              // 실브랜드 0: 데모 더미 7개
}, [userBrands]);
```

brand(active) 재정의: 목록에서 active 우선, 없으면 더미, 그래도 없으면 폴백.
```
const brand = React.useMemo<Brand>(() => {
  const u = userBrands.find(b => b.id === brandId);
  if (u) return toBrand(u);
  return getBrand(brandId) ?? (userBrands[0] ? toBrand(userBrands[0]) : brands[0]);
}, [brandId, userBrands]);
```
active 폴백: active 가 allBrands 에 없으면 첫 브랜드로 — 위 brand useMemo 가 이미 폴백 처리(find 실패 시 getBrand/첫브랜드). 추가로 effect 불필요.

isAgency: `const isAgency = userBrands.length >= 2;` (SSR-safe 노출은 아래 value 에서 mounted 가드.)

addBrand:
```
const addBrand = React.useCallback((data: UserBrandData) => {
  addUserBrand(data);                 // 영속화(목록 upsert)
  refreshUserBrand();                 // userBrands/userBrand 재로드
  setBrandIdState(data.id);           // 신규를 active 로
  if (typeof window !== "undefined") {
    localStorage.setItem(STORAGE_KEY, data.id);
    window.dispatchEvent(new Event("briq:user-brand-updated"));
  }
}, [refreshUserBrand]);
```

value 에 SSR 가드 추가:
```
userBrands: mounted ? userBrands : [],
isAgency: mounted ? isAgency : false,
addBrand,
```
(기존 brand/brandId/allBrands/userBrand 의 mounted 가드 패턴 유지.)
  </action>
  <verify>
    <automated>cd briq-app && ./node_modules/.bin/tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <done>tsc 0 에러. Ctx 에 userBrands/isAgency/addBrand 노출. allBrands 가 실브랜드>0 이면 실브랜드만, 0 이면 더미 7개. setBrandId 가 목록+더미 모두 탐색.</done>
</task>

<task type="auto">
  <name>Task 3: BrandSwitcher 신규 컴포넌트 + Sidebar 유저 플레이트 교체</name>
  <files>briq-app/components/layout/BrandSwitcher.tsx, briq-app/components/layout/Sidebar.tsx</files>
  <action>
신규 components/layout/BrandSwitcher.tsx ("use client"):
- import: useBrand, Link 불필요(router 사용), useRouter(next/navigation), lucide ChevronDown/Check/Plus, tokens(INK, INK_MUTE, RULE, PAPER, PAPER_HOVER, SAGE).
- props: `{ onNavigate?: () => void }` (모바일 닫기용).
- 소비: `const { allBrands, brand, setBrandId } = useBrand(); const [open, setOpen] = React.useState(false); const router = useRouter();`
- 디자인 철칙(CLAUDE.md): 헤어라인 사각(`border: 0.5px solid RULE`), rounded-xl/그라데이션/SaaS색 금지, 한글 이탤릭 금지, 한글 라벨 tracking 0.01~0.02em·uppercase 금지. 색은 INK/INK_MUTE/PAPER/SAGE 만. lucide 아이콘.
- 분기:
  - `allBrands.length >= 2`: 트리거 버튼 = 현재 `brand.name` + ChevronDown(h-3.5). 클릭 → setOpen(toggle). open 시 드롭다운(absolute, 종이 PAPER 배경, 0.5px RULE 테두리):
    - 브랜드 목록 map: 각 행 버튼 → 클릭 시 `setBrandId(b.id); setOpen(false);`. 현재 active(`b.id === brand.id`)면 우측 Check(h-3.5, color SAGE). hover 배경 PAPER_HOVER.
    - 구분선(0.5px RULE) 한 줄.
    - "＋ 브랜드 추가" 행(Plus 아이콘) → 클릭 시 `setOpen(false); onNavigate?.(); router.push("/onboarding?add=1");`.
  - `allBrands.length === 1` (또는 length 0 안전): 드롭다운 없이 `brand.name` 텍스트만(ChevronDown 없음).
- 바깥 클릭 닫기: useRef + document mousedown 리스너(open 일 때만). ESC 닫기 선택.
- 접근성: 트리거 `aria-haspopup="menu" aria-expanded={open}`, 드롭다운 role="menu".
- 폭: Sidebar 유저 영역에 맞게 `w-full`, 텍스트 `truncate`.
- 요금제 게이팅 자리표시 주석: "＋ 브랜드 추가" 위에 `{/* TODO(billing): 플랜별 브랜드 수 상한 게이팅 지점 — 이번 범위 밖 */}`.

Sidebar.tsx 수정:
- import BrandSwitcher 추가: `import { BrandSwitcher } from "./BrandSwitcher";`
- useBrand 소비에 변화 없음(스위처가 자체 소비). 단 하단 플레이트의 `shopSummary` 정적 텍스트 줄(@36-40)과 표시 영역(@115-118) 을 BrandSwitcher 로 교체.
  - 구체: 하단 user 영역(@111-134) 의 `<div className="flex-1 min-w-0">...displayName/shopSummary...</div>` 블록을 다음으로 교체 —
    ```
    <div className="flex-1 min-w-0 flex flex-col gap-1">
      <div className="text-[12px] font-medium truncate">{displayName}</div>
      <BrandSwitcher onNavigate={onNavigate} />
    </div>
    ```
    (아바타 이니셜·설정 톱니·ThemeToggle 은 그대로 유지. displayName 로직 @33 유지.)
  - 사용하지 않게 되는 `otherCount`/`shopSummary` 계산(@35-40) 제거(미사용 lint 방지).
- 기존 Sidebar 의 구 zinc/emerald 색은 건드리지 말 것(별도 작업). BrandSwitcher 내부만 에디토리얼 토큰.
- 모바일/데스크톱 공유 SidebarBody 라서 양쪽 자동 적용 — 별도 작업 불필요.
  </action>
  <verify>
    <automated>cd briq-app && ./node_modules/.bin/tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <done>tsc 0. BrandSwitcher 가 allBrands>=2 면 드롭다운+브랜드추가, ==1 이면 단일 텍스트. Sidebar 하단 플레이트가 정적 텍스트 대신 BrandSwitcher 렌더. 미사용 otherCount/shopSummary 제거.</done>
</task>

<task type="auto">
  <name>Task 4: nav agencyOnly 게이팅 + 온보딩 append 모드</name>
  <files>briq-app/lib/nav.ts, briq-app/components/layout/Sidebar.tsx, briq-app/components/onboarding/Onboarding.tsx</files>
  <action>
nav.ts:
- NavItem 타입에 `/** 대행사(실브랜드 2+)에서만 노출 */ agencyOnly?: boolean;` 추가.
- 클라이언트 항목을 메뉴에 노출. HIDDEN_ROUTES 의 `{ label: "Clients", href: "/clients", ... hidden:true }` 는 그대로 두되(라우트 유지), navGroups 운영 그룹에 클라이언트 항목 추가:
  - 첫 그룹 items 배열의 "검수" 다음에 `{ label: "클라이언트", href: "/clients", icon: <적절 lucide — Briefcase 권장>, agencyOnly: true },` 추가. (Briefcase 를 lucide import 에 추가.)
  - 주의: HIDDEN_ROUTES 의 Clients(hidden:true)와 href 중복 → NAV_FLAT 에 /clients 가 2회 들어갈 수 있음. NAV_FLAT 은 라우트 평탄화용이므로 중복 무해하나, 깔끔히 하려면 HIDDEN_ROUTES 에서 Clients 줄 제거(navGroups 가 대신 노출). 제거 권장.

Sidebar.tsx navGroups 렌더 필터(@69 group.items.map 진입부):
- useBrand 소비에 `isAgency` 추가: `const { userBrand, allBrands, brand, isAgency } = useBrand();`
- map 전에 필터: `group.items.filter(item => !item.hidden && !(item.agencyOnly && !isAgency)).map(...)`. (현재는 hidden 필터가 명시적이지 않으니 — 현 코드는 navGroups 에 hidden 항목이 없어 그냥 map. agencyOnly 도입 시 명시 필터 추가.)
- 결과: 솔로(isAgency false)면 클라이언트 숨김, 대행사(true)면 노출.
- 데모(실브랜드 0→더미)에서의 노출 요구사항: 검증 시나리오 (a) 는 "데모면 클라이언트 노출(대행사 뷰)". 그러나 isAgency 는 userBrands.length>=2 라 데모(userBrands=0)면 false → 클라이언트 숨김. 요구 충돌 해소: Sidebar 게이팅 조건을 `isAgency || allBrands.length >= 2` 로 한다(더미 7개 데모도 allBrands>=2 → 노출). 즉 필터식: `!(item.agencyOnly && !(isAgency || allBrands.length >= 2))`. 솔로(실1)는 allBrands=1 이라 숨김 유지, 데모(더미7)·대행사(실2+)는 노출.

Onboarding.tsx append 분기:
- import 변경: `import { useBrand } from "@/components/brand/BrandProvider";` 추가. 컴포넌트 내 `const { addBrand } = useBrand();`.
- `const addMode = searchParams?.get("add") === "1";` (planParam 옆 @235 근처).
- 완료 핸들러 @1079 onClick 내부, `saveUserBrand({...})` 호출 부분을 분기:
  ```
  const payload = { id: brandId, name: ..., ...기존 그대로 };
  if (addMode) {
    addBrand(payload);          // 목록 추가 + 활성화 + dispatch (BrandProvider 가 처리)
  } else {
    saveUserBrand(payload);
    if (typeof window !== "undefined") window.dispatchEvent(new Event("briq:user-brand-updated"));
  }
  ```
  (payload 를 변수로 빼서 중복 제거. 기존 selectedPlan 분기 router.push 는 그대로.)
- ClientsScreen / BrandSwitcher 의 "브랜드 추가" 진입 경로: ClientsScreen.tsx @59 `router.push("/onboarding")` → `router.push("/onboarding?add=1")` 로 변경. (BrandSwitcher 는 Task3 에서 이미 ?add=1.)
- add 모드일 때 결제 분기: addMode 면 보통 무료/추가이므로 selectedPlan 기본값이 결제로 안 새도록 — 기존 동작 유지하되, add 모드에서 selectedPlan 이 'free'/미선택이면 router.push("/dashboard") 가 자연히 동작. 별도 강제 불필요.
  </action>
  <verify>
    <automated>cd briq-app && ./node_modules/.bin/tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <done>tsc 0. nav.ts 에 agencyOnly + 클라이언트 항목. Sidebar 가 솔로(실1)면 클라이언트 숨김, 데모(더미)·대행사(실2+)면 노출. 온보딩 ?add=1 이면 addBrand 로 추가·활성화, 일반 진입은 기존 saveUserBrand 유지. ClientsScreen 추가 버튼이 ?add=1 진입.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: 상태별 nav 육안 검증 (데모/솔로/대행사)</name>
  <what-built>다중 브랜드 모델, BrandSwitcher, agencyOnly nav 게이팅, 온보딩 append. 실행자는 dev 서버(3000)를 띄우고 localStorage seed 를 주입하는 임시 디버그 라우트 `app/_debug-seed/page.tsx`(robots noindex, "use client") 를 만들어 세 상태를 토글한다.</what-built>
  <how-to-verify>
임시 seed 라우트 (실행자가 생성, 검증 후 삭제):
- app/_debug-seed/page.tsx — 버튼 3개:
  (a) "데모": localStorage.removeItem("briq:user-brands"); removeItem("briq:user-brand"); removeItem("briq-active-brand"); → 실브랜드 0.
  (b) "솔로": saveUserBrands([brand1]) 1개 seed (palette ≥1, industry 유효) + active 그 id.
  (c) "대행사": saveUserBrands([brand1, brand2]) 2개 seed + active brand1.
  각 버튼 후 location.href="/dashboard".

헤드리스 스크린샷 (3 상태 각각):
1. dev 서버: `cd briq-app && pnpm dev` (또는 npm run dev) — 포트 3000.
2. /_debug-seed 에서 상태 버튼 클릭 → /dashboard 진입 후:
   `"/Applications/Google Chrome.app/Contents/MacOS/Google Chrome" --headless=new --disable-gpu --window-size=1440,900 --screenshot=/tmp/nav-demo.png http://localhost:3000/dashboard` (seed 는 localStorage라 헤드리스 별도 세션이면 evaluate 불가 → 대안: 임시 라우트가 querystring `?seed=demo|solo|agency` 를 읽어 seed 후 redirect 하도록 구현. 헤드리스로 `/_debug-seed?seed=agency` 직접 호출).
3. 기대:
   (a) 데모(더미7): Sidebar 스위처에 드롭다운(▼) + "클라이언트" 메뉴 보임.
   (b) 솔로(실1): 스위처 단일 텍스트(드롭다운 없음) + "클라이언트" 메뉴 안 보임.
   (c) 대행사(실2): 스위처 드롭다운 + "클라이언트" 메뉴 보임. 드롭다운 클릭 시 2개 브랜드 + Check + "＋ 브랜드 추가".
4. 디자인 육안: BrandSwitcher 가 INK/INK_MUTE/RULE/PAPER 톤(에디토리얼), 그라데이션·rounded-xl·한글 이탤릭 없음.
검증 후 app/_debug-seed/ 삭제.
  </how-to-verify>
  <resume-signal>세 상태 스크린샷이 기대대로면 "approved", 아니면 어긋난 상태/항목 기술</resume-signal>
</task>

</tasks>

<verification>
- `cd briq-app && ./node_modules/.bin/tsc --noEmit -p tsconfig.json` → 0 에러.
- `cd briq-app && ./node_modules/.bin/vitest run lib/brand/user-brand.test.ts` → 통과.
- 상태별 헤드리스 스크린샷 3장(데모/솔로/대행사) 기대 일치.
- 워크트리 미사용.
</verification>

<success_criteria>
- loadUserBrands/addUserBrand/removeUserBrand/saveUserBrands 동작 + 단일→다중 1회 마이그레이션.
- BrandProvider 가 userBrands/isAgency/addBrand 노출, allBrands 가 실브랜드>0 이면 실브랜드만(더미 제외)·0 이면 더미.
- BrandSwitcher 가 브랜드 수에 적응(2+ 드롭다운, 1 단일), 에디토리얼 토큰 준수.
- nav 가 솔로(실1)면 클라이언트 숨김, 데모·대행사(2+)면 노출.
- 온보딩 ?add=1 이 기존 브랜드 무손상 + 신규 추가·활성화. 기존 최초 온보딩 동작 무손상.
</success_criteria>

<output>
After completion, create `.planning/quick/260608-0ux-agency-adaptive-nav/260608-0ux-SUMMARY.md`
</output>
