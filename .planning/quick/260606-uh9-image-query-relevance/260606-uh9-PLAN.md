---
phase: quick-260606-uh9
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - briq-app/lib/cardnews/hook-generator.ts
autonomous: true
requirements: [QUICK-260606-uh9]
must_haves:
  truths:
    - "카드뉴스 CTA/problem/proof 슬라이드 쿼리에 'welcoming entrance'·'quiet corner'·'candid' 등 장소어/모순어가 없다"
    - "value 슬라이드 3·4·5의 imageQuery 가 서로 다르다 (3가지 구도)"
    - "음식/상품 토픽 키워드(cake/mango/stollen)가 쿼리 앞쪽에 유지된다"
    - "모든 역할 쿼리에 'no people' 이 유지된다"
    - "tsc --noEmit 가 0 으로 통과한다"
  artifacts:
    - path: "briq-app/lib/cardnews/hook-generator.ts"
      provides: "imageQueryFor 피사체 구도 기반 역할 모디파이어 + value variant"
      contains: "VALUE_COMPOSITIONS"
  key_links:
    - from: "슬라이드 빌더 value 3개 호출 (@953/958/963)"
      to: "imageQueryFor(role, ctx, variant)"
      via: "variant 0/1/2 인자 전달"
      pattern: "imageQueryFor\\(\"value\", ctx, [012]\\)"
---

<objective>
카드뉴스 슬라이드 사진이 주제·피사체와 무관하게 반복되는 문제를 imageQueryFor 의 역할 모디파이어 교체로 해결.

근본 원인(프로브 확인): 역할 모디파이어가 "장소어"(welcoming entrance, quiet corner)·"모순어"(candid vs no people) 기반이라 음식/상품 피사체를 밀어내고 빈 테이블·입구·양초 같은 앰비언트 사진을 반복. 게다가 value 슬라이드 3·4·5 가 완전히 동일한 쿼리라 한 캠페인에서 같은 사진 반복.

Purpose: 사진을 주제·피사체 중심으로 되돌려 카드뉴스 관련성 회복.
Output: hook-generator.ts 1파일 수정 (imageQueryFor 역할 모디파이어 + value variant 분배).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/quick/260606-uh9-image-query-relevance/260606-uh9-PLAN.md

<interfaces>
From components/campaigns/types.ts:
```typescript
export type SlideRole = "hook" | "problem" | "value" | "proof" | "cta";
```

현재 imageQueryFor 시그니처 (hook-generator.ts @661):
`function imageQueryFor(role: SlideRole, ctx: Ctx): string`

호출부 6곳 (슬라이드 빌더 @940~977):
- @945  imageQueryFor("hook", ctx)
- @951  imageQueryFor("problem", ctx)
- @956  imageQueryFor("value", ctx)   ← value 1 (slide 3)
- @961  imageQueryFor("value", ctx)   ← value 2 (slide 4)
- @967  imageQueryFor("value", ctx)   ← value 3 (slide 5)
- @972  imageQueryFor("proof", ctx)
- @976  imageQueryFor("cta", ctx)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: imageQueryFor 역할 모디파이어를 피사체 구도어로 교체 + value variant 추가</name>
  <files>briq-app/lib/cardnews/hook-generator.ts</files>
  <action>
imageQueryFor 함수(@661~695)를 두 가지로 고친다. 다른 호출부(hook/problem/proof/cta)는 variant 옵셔널이라 무손상. industryScene/moodStyle/tail/subject 결합 로직(@662~680)은 그대로 둔다 ('no people' 유지, 토픽 subject 우선 결합 유지 — 보수적, industrySubject 비중 변경 없음).

**EDIT 1 — 시그니처에 옵셔널 variant 추가.**
OLD (@661):
```
function imageQueryFor(role: SlideRole, ctx: Ctx): string {
```
NEW:
```
// value 슬라이드(3·4·5) 전용 3구도 — 한 캠페인에서 같은 사진 반복 방지.
const VALUE_COMPOSITIONS = [
  "macro detail close up",
  "overhead flat lay",
  "side angle soft light",
] as const;

function imageQueryFor(role: SlideRole, ctx: Ctx, variant = 0): string {
```

**EDIT 2 — switch 의 역할 모디파이어 교체.** 장소어/모순어 제거, 음식·상품·시술·공간 6업종에 모두 자연스러운 피사체 구도어로.
OLD (@682~694):
```
  // 역할별 구도만 지정 — 톤/분위기는 mood(tail)가 결정하도록 고정 톤 단어 제거
  switch (role) {
    case "hook":
      return `${subject} hero overhead composition, ${tail}`;
    case "problem":
      return `${subject} quiet corner, ${tail}`;
    case "value":
      return `${subject} detail close up, ${tail}`;
    case "proof":
      return `${subject} intimate scene, candid, ${tail}`;
    case "cta":
      return `${subject} welcoming entrance, ${tail}`;
  }
```
NEW:
```
  // 역할별 "피사체 구도어"만 지정 — 장소어·모순어(candid vs no people) 제거.
  // 톤/분위기는 mood(tail)가 결정. 음식·상품·시술·공간 6업종에 모두 자연스러운 구도 용어.
  switch (role) {
    case "hook":
      return `${subject} hero overhead composition, ${tail}`;
    case "problem":
      return `${subject} moody side angle, soft shadow, ${tail}`;
    case "value":
      // 3·4·5 슬라이드를 위해 variant(0/1/2)로 3구도 분배.
      return `${subject} ${VALUE_COMPOSITIONS[variant % VALUE_COMPOSITIONS.length]}, ${tail}`;
    case "proof":
      return `${subject} styled still life, ${tail}`;
    case "cta":
      return `${subject} styled flat lay, ${tail}`;
  }
```

**EDIT 3 — value 호출부 3곳에 variant 전달.** (hook/problem/proof/cta 호출부는 건드리지 않는다 — variant 기본값 0.)
OLD (@956):  `      imageQuery: imageQueryFor("value", ctx) },`
NEW (@956):  `      imageQuery: imageQueryFor("value", ctx, 0) },`

OLD (@961):  `      imageQuery: imageQueryFor("value", ctx) },`
NEW (@961):  `      imageQuery: imageQueryFor("value", ctx, 1) },`

OLD (@967):  `      imageQuery: imageQueryFor("value", ctx) },`
NEW (@967):  `      imageQuery: imageQueryFor("value", ctx, 2) },`

주의: @956/961/967 세 줄은 텍스트가 동일하므로 Edit 시 충분한 앞뒤 컨텍스트(caption: values[0/1/2] 라인 포함)를 잡아 각각 구분해 수정할 것. 또는 슬라이드 객체 블록 단위로 교체.
  </action>
  <verify>
    <automated>cd briq-app && ./node_modules/.bin/tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <done>
tsc 0 통과. switch 에 welcoming entrance / quiet corner / candid 문자열이 사라지고, VALUE_COMPOSITIONS 배열 존재, value 호출부 3곳이 variant 0/1/2.
  </done>
</task>

<task type="auto">
  <name>Task 2: jiti 프로브로 6업종×3토픽 쿼리 검증 (assert PASS/FAIL)</name>
  <files>briq-app/scripts/probe-image-query.mjs</files>
  <action>
generateCardnewsCampaign 를 6업종(restaurant/cafe/dessert/beauty/stay/local — dolce-dessert 포함)×토픽 3종으로 호출해 7슬라이드 imageQuery 를 출력·검증하는 1회용 jiti 프로브 작성.

토픽 예시(업종 무관 공통 3종 + 음식 키워드 확인용): ["시즌 딸기 케이크", "여름 망고 빙수", "크리스마스 슈톨렌"]. brand 객체는 generateCardnewsCampaign 시그니처(@867)와 ctx 빌드(@878)를 읽어 최소 필드(industry, mood 등)만 채운다. mood 는 "warm" 고정.

jiti 로 TS 직접 로드:
```
import { createJiti } from "jiti";
const jiti = createJiti(import.meta.url, { alias: { "@": new URL("../", import.meta.url).pathname } });
const mod = await jiti.import("./lib/cardnews/hook-generator.ts");
```
('@' alias 를 briq-app 루트로 매핑 — types.ts import 해소용. jiti 미설치 시 `pnpm add -D jiti` 후 진행.)

각 (업종,토픽) 조합마다 7개 imageQuery 를 모아 다음 assert:
- (a) 어떤 쿼리에도 "welcoming entrance" | "quiet corner" | "candid" 가 없다.
- (b) value 슬라이드(role==="value", slide 3·4·5)의 쿼리 3개가 서로 다르다 (Set size === 3).
- (c) 음식 토픽일 때 쿼리 앞쪽(첫 60자)에 토픽 키워드(cake/mango/stollen) 중 해당 단어가 포함.
- (d) 모든 쿼리에 "no people" 포함.
각 조합 결과를 한 줄로 출력하고, 마지막에 전체 PASS/FAIL 요약. 하나라도 실패면 process.exit(1).
  </action>
  <verify>
    <automated>cd briq-app && node scripts/probe-image-query.mjs</automated>
  </verify>
  <done>
프로브가 6업종×3토픽 전부 (a)(b)(d) PASS, 음식 토픽에서 (c) PASS 출력하고 exit 0. (검증 후 scripts/probe-image-query.mjs 는 남겨두거나 삭제 — 실행자 판단.)
  </done>
</task>

</tasks>

<verification>
1. `cd briq-app && ./node_modules/.bin/tsc --noEmit -p tsconfig.json` → 0.
2. `cd briq-app && node scripts/probe-image-query.mjs` → 전 조합 PASS, exit 0.
3. (선택) dev서버(3000) 헤드리스로 dolce-dessert CTA 슬라이드 사진이 입구/앰비언트 → 케이크류로 바뀌는지 육안.
</verification>

<success_criteria>
- imageQueryFor 역할 모디파이어가 피사체 구도어 기반 (장소어/모순어 제거).
- value 3·4·5 쿼리 차별화 (3구도).
- 음식 토픽 키워드 쿼리 앞쪽 유지, 'no people' 유지.
- 다른 호출부(hook/problem/proof/cta)·카피·과금·발행 로직 무손상 (variant 옵셔널).
</success_criteria>

<output>
After completion, create `.planning/quick/260606-uh9-image-query-relevance/260606-uh9-SUMMARY.md`.

SUMMARY 에 반드시 한 줄 남길 것:
"추후 search-pexels 라우트의 top5 Math.random pick 을 deterministic pickIndex(seed, slideId) 로 바꾸면 결정성·슬라이드 간 사진 차별을 더 강화할 수 있음 (이번 범위 밖)."
</output>
