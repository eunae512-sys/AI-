---
phase: quick-260609-1a5
plan: 01
subsystem: ui
tags: [shorts, voice-bank, copywriting, editable-fields, react-state, honesty]

requires:
  - phase: quick-260608-2mh
    provides: seed 기반 추천 씬(쇼츠/릴스 brand.id seed)
provides:
  - 정돈된 사장님 구어체 HOOK/MID/CTA/해시태그 뱅크(저렴한 바이럴 톤 전량 제거)
  - 쇼츠 결과 카피(자막·제목·본문) 인라인 편집 + 자막 전용 필드
  - 편집값이 발행 미리보기 번인 자막에 라이브 반영
affects: [shorts, reels, voice-bank, copy-tone]

tech-stack:
  added: []
  patterns:
    - "FieldBlock onChange 옵션 → 토큰 정합(RULE/INK/PAPER/사각) 인라인 편집 인풋, 미제공 시 읽기전용 폴백"
    - "updateResult(platform, patch) → setResults map 으로 결과 state 단일 출처(복사·미리보기 공유)"

key-files:
  created: []
  modified:
    - briq-app/lib/viral/voice-bank.ts
    - briq-app/components/shorts/ShortsScreen.tsx
    - briq-app/lib/dummy/shorts-presets.ts

key-decisions:
  - "번인 자막은 후크 한 줄을 그대로 쓰되 — 라벨 꼬리만 제거(문장 내 쉼표 보존) — 정돈 구어체에서 '여기' 깨짐 해소"
  - "tiktok 해시태그의 #fyp/#포유 하드코딩 제거 — voice-bank 외에서 새던 금지어 차단(정직성)"
  - "자막 필드는 naver 제외(미리보기 번인과 일치) — 노출 위치와 데이터 사용처 일치"

patterns-established:
  - "FieldBlock: onChange 유무로 편집/읽기 분기 — 토큰 정합 인풋"
  - "결과 state(updateResult)가 복사·번인 미리보기의 단일 출처"

requirements-completed: [COPY-REFINE-01, COPY-EDITABLE-02]

duration: ~40min
completed: 2026-06-09
---

# Phase quick-260609-1a5: 쇼츠 추천 카피 정돈 + 편집 가능 Summary

**voice-bank 카피 풀을 저렴한 바이럴 클릭베이트에서 정돈된 사장님 구어체로 전면 교체하고, 쇼츠 결과의 자막·제목·본문을 인라인 편집 가능하게(자막 필드 신설·편집값 번인 미리보기 라이브 반영) 만들었다.**

## Performance

- **Duration:** ~40 min
- **Tasks:** 2 (+ 1 범위 내 자동수정)
- **Files modified:** 3

## Accomplishments
- HOOK_BANK(4 grammar × 6 업종 × 8) · MID_BANK · CTA_BANK · VIRAL_HASHTAGS 전면 정돈 — 금지어 0
- 쇼츠 결과 카피 인라인 편집(updateResult + FieldBlock onChange) + "자막 (영상 위 문구)" 필드 신설
- 자막 편집이 PlatformCard 번인 미리보기에 라이브 반영(3개 브랜드 CDP 검증 통과)
- reels 회귀 없음(같은 voice-bank 사용, 톤 함께 개선)

## Task Commits

1. **Task 1: voice-bank 카피 정돈** - `a6194e8` (fix)
2. **Task 2: ShortsScreen 편집 가능 + 자막 필드 (+ shorts-presets 번인/해시태그 보정)** - `6421537` (feat)

## Files Created/Modified
- `briq-app/lib/viral/voice-bank.ts` — HOOK/MID/CTA/해시태그 풀 정돈, 상단 주석 톤 수정. export 시그니처·슬롯·배열 길이 무변경
- `briq-app/components/shorts/ShortsScreen.tsx` — updateResult, PlatformCard onUpdate, FieldBlock onChange(편집 인풋), 자막 필드 신설
- `briq-app/lib/dummy/shorts-presets.ts` — reels 번인 자막 쉼표 보존, tiktok #fyp/#포유 제거

## 카피 정돈 — before → after (대표)

| before (저렴/클릭베이트) | after (정돈된 사장님 구어체) |
|---|---|
| "{city} 와서 여기 안 가면 손해임" | "{city} 가면 한 번은 들르게 되는 곳" |
| "저장각 — 다음 주말에 갈 곳" | "다음 주말, 여기 어때요" |
| "도착하자마자 사진 백 장 찍게 되는 곳" | "문 열고 들어서면 먼저 사진부터 찍게 돼요" |
| "여기 디저트 한 입 먹고 다시 옴" | "한 입 먹고 다음에 또 생각나는 디저트" |
| CTA "저장 안 하면 후회 가능" | CTA "다음에 들를 때 떠올려 주세요" |
| 해시태그 ["저장각","찐맛집","fyp","포유"...] | ["동네맛집","주말나들이","로컬추천","오늘기록"...] |

## 검증 결과

- **tsc:** `./node_modules/.bin/tsc --noEmit -p tsconfig.json` → exit 0 (3회: voice-bank 후 / ShortsScreen 후 / shorts-presets 보정 후)
- **금지어 grep:** `grep -nE "저장각|찐맛집|손해임|사진 100장|사진 백 장|박아|표정 (변|바뀌)|무조건|1티어|줄 서서|놓치지|fyp|포유" voice-bank.ts` → 0건(exit 1)
- **렌더 fyp/포유 grep:** 3브랜드 생성 결과(report.json)에 `#fyp`/`#포유` 0건
- **/shorts CDP(헤드리스 Chrome 9222, 3브랜드 miokdang·roastery-1985·seochon-stay):**
  - 자막/제목/본문 모두 `<textarea>`/`<input>` 편집 인풋으로 렌더(토큰 정합 PAPER bg·RULE 보더·사각)
  - 자막 input 편집("테스트 자막 — 편집 라이브 반영") → 번인 미리보기 `<p>` 가 즉시 일치(burnInMatch 3/3 PASS)
  - 기본 자막이 정돈된 한 문장으로 출력("혼자 와도 편하게 한 끼 드시고 가세요" / "다음 주말, 여기 어때요" / "아침 햇살 들어올 때가 가장 좋아요")
- **전체 복사:** fullText 가 `result.title/caption/hashtags`(=updateResult 가 갱신하는 state) 에서 계산 → 편집값이 복사 출처임을 코드·렌더로 확인
- **/reels 스모크:** roastery-1985 로드 → errorOverlay 없음, hook 추천이 정돈 구어체로 출력("주말 오전엔 한산해서 좋아요" 등). 스크린샷 육안 정상

## 카피 품질 판단 (한국어 카피라이터 관점)
3개 브랜드 출력 전수 육안 — 모든 라인이 "사장님이 직접 쓴 단정한 한 줄"로 읽힘. 번역체·클릭베이트·과장 수치 없음. 업종/플랫폼 결 유지(reels 잔잔, tiktok 짧고 담백, shorts 검색형, naver 지역형). 재정돈 불필요.

## Decisions Made
- 번인 자막 절단 정규식을 `[—,]` → `—` 로 변경: 정돈 구어체 후크가 문장 내 쉼표를 쓰므로 쉼표 절단 시 "여기"처럼 깨짐. 라벨형 em-dash 꼬리만 제거.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] reels 번인 자막이 쉼표에서 절단되어 "여기"로 깨짐**
- **Found during:** Task 2 (CDP 검증)
- **Issue:** shorts-presets.ts 의 `subtitle = pickedHook.replace(/[—,].+$/, "")` 가 정돈된 후크("여기, 결이 조금 달라요")를 첫 쉼표에서 잘라 번인 자막이 "여기"로 출력 — 내 카피 변경이 직접 유발
- **Fix:** 절단 정규식을 `/\s*—.+$/` 로(em-dash 라벨 꼬리만 제거, 문장 내 쉼표 보존)
- **Files modified:** briq-app/lib/dummy/shorts-presets.ts
- **Verification:** CDP 재실행 — 번인 자막이 완전한 문장으로 출력
- **Committed in:** `6421537`

**2. [Rule 2 - Honesty/금지어] tiktok 해시태그 #fyp/#포유 하드코딩 잔존**
- **Found during:** Task 2 (해시태그 렌더 점검)
- **Issue:** PLAN/CLAUDE.md 가 fyp·포유를 금지하나 voice-bank 가 아닌 shorts-presets.ts 의 tiktok 케이스에서 `[...allHashtags.slice(0,3), "#fyp", "#포유"]` 로 하드코딩되어 출력에 그대로 노출
- **Fix:** `allHashtags.slice(0, meta.hashtagCount)`(=5개) 로 교체, 담백한 동네·기록형 태그만 사용
- **Files modified:** briq-app/lib/dummy/shorts-presets.ts
- **Verification:** 3브랜드 렌더 결과 `#fyp`/`#포유` 0건
- **Committed in:** `6421537`

---

**Total deviations:** 2 auto-fixed (1 bug, 1 honesty/금지어)
**Impact on plan:** 둘 다 카피 정돈 목표의 직접 결과 — 정돈된 카피가 기존 파이프라인 로직과 만나며 드러난 결함. shorts-presets.ts 는 PLAN files_modified 에 없었으나 쇼츠 카피 산출 파이프라인의 일부로 범위 내. scope creep 아님.

## Issues Encountered
- 별도 dev 서버(포트 3000)가 이미 구동 중이라 신규 3210 서버 기동 실패 → 기존 3000 서버 재사용으로 검증 진행. 영향 없음.
- puppeteer/playwright/ws 미설치 → Node 내장 net/crypto 로 최소 CDP WebSocket 클라이언트(/tmp/cdp-shorts-1a5.mjs) 직접 구현해 검증. 임시 파일(스크립트·스크린샷·report)은 /tmp 에 남김.

## Known Stubs
None — 쇼츠 카피는 데모 생성(GPT 미연동) 이나 이는 기존 설계(800ms 인공 지연, "실서비스는 GPT 호출" 주석)로 본 작업 범위 밖. 신규 stub 없음.

## User Setup Required
None.

## Next Phase Readiness
- 쇼츠 추천 카피 정돈·편집 완료. 후속 가능: regenerate 시 편집값 보존 옵션(현재는 새 카피로 덮어씀=의도), naver 소식 본문도 편집 가능화는 이미 적용됨(자막만 naver 제외).

## Self-Check: PASSED

- Files: voice-bank.ts / ShortsScreen.tsx / shorts-presets.ts / 260609-1a5-SUMMARY.md — all FOUND
- Commits: `a6194e8`, `6421537` — both FOUND in git log

---
*Phase: quick-260609-1a5-shorts-copy-refine*
*Completed: 2026-06-09*
