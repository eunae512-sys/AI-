---
phase: quick-260608-d9z
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - briq-app/lib/content/seasonal-topics.ts
  - briq-app/components/blog/BlogScreen.tsx
  - briq-app/components/cardnews/CardnewsScreen.tsx
  - briq-app/components/dashboard/Dashboard.tsx
autonomous: true
requirements: [SEASON-SHARE, CARDNEWS-SEASON, DASH-SEASON]

must_haves:
  truths:
    - "시즌 추천 주제가 단일 소스(lib/content/seasonal-topics.ts)에서 나온다"
    - "BlogScreen 추천 결과가 추출 전과 동일하다(회귀 0)"
    - "CardnewsScreen 주제 placeholder 가 현재 시즌(6월=여름)을 반영하고 10~12월 undefined 버그가 없다"
    - "Dashboard '이번 시즌 이벤트' desc 가 현재 시즌(6월=여름)을 반영하고 static '어버이날' 단독 노출이 없다"
  artifacts:
    - path: "briq-app/lib/content/seasonal-topics.ts"
      provides: "SEASONAL_TOPICS(72) + Topic 타입 + getSeasonalTopics/getSeasonalTopicTitles"
      contains: "export function getSeasonalTopics"
    - path: "briq-app/components/blog/BlogScreen.tsx"
      provides: "신규 lib 에서 import (로컬 SEASONAL_TOPICS 제거)"
      contains: "lib/content/seasonal-topics"
    - path: "briq-app/components/cardnews/CardnewsScreen.tsx"
      provides: "시즌 인식 topicPlaceholder (버그 월 매트릭스 제거)"
      contains: "getSeasonalTopicTitles"
    - path: "briq-app/components/dashboard/Dashboard.tsx"
      provides: "시즌 인식 event SuggestedMove"
      contains: "getSeasonContext"
  key_links:
    - from: "briq-app/components/blog/BlogScreen.tsx"
      to: "briq-app/lib/content/seasonal-topics.ts"
      via: "getSeasonalTopics import + presets useMemo"
      pattern: "getSeasonalTopics"
    - from: "briq-app/components/cardnews/CardnewsScreen.tsx"
      to: "briq-app/lib/content/seasonal-topics.ts"
      via: "getSeasonalTopicTitles in topicPlaceholder useMemo"
      pattern: "getSeasonalTopicTitles"
    - from: "briq-app/components/dashboard/Dashboard.tsx"
      to: "briq-app/lib/content/season.ts"
      via: "getSeasonContext in suggestionsForIndustry"
      pattern: "getSeasonContext"
---

<objective>
시즌 인식을 카드뉴스 주제 placeholder·대시보드 "이번 시즌 이벤트"로 확장하고,
블로그에 박혀 있던 SEASONAL_TOPICS(72개) + Topic 타입을 신규 공유 lib(`lib/content/seasonal-topics.ts`)로
추출해 단일 소스화한다.

Purpose: 시즌 주제가 화면마다 파편화(블로그는 season.ts 기반, 카드뉴스는 버그 있는 자체 월 매트릭스,
대시보드는 static "어버이날" 단독)되어 있고, 특히 카드뉴스 월 매트릭스에 인덱싱 버그(10~12월 undefined)와
오정렬(stay 항목 혼입)이 있다. 단일 소스로 합쳐 버그를 제거하고 시즌을 정확히 반영한다.

Output: 신규 공유 lib 1개 + 3개 화면 전환(블로그는 결과 동일 회귀 0, 카드뉴스·대시보드는 시즌 인식).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md

<interfaces>
<!-- 단일 시즌 표준. 새 코드는 autumn 사용("fall" 금지). -->

From briq-app/lib/content/season.ts:
```typescript
export type SeasonKey = "spring" | "summer" | "autumn" | "winter";
export interface SeasonContext {
  month: number;        // 1~12 (KST)
  seasonKey: SeasonKey;
  seasonLabel: string;  // 봄/여름/가을/겨울
  monthLabel: string;   // "N월"
}
export function getSeasonContext(now?: Date): SeasonContext;
// 월→시즌: 3~5 봄 / 6~8 여름 / 9~11 가을 / 12·1·2 겨울. 2026-06 → summer.
```

From briq-app/components/blog/BlogScreen.tsx (추출 대상 — 그대로 옮긴다):
```typescript
type Topic = { title: string; keywords: string[]; intent: string };
// const SEASONAL_TOPICS: Record<string, Record<SeasonKey, Topic[]>>  (@20-153)
//   업종 키: restaurant, cafe, dessert, stay, beauty, local  (6 업종)
//   각 업종 × 4 시즌 × 3 주제 = 72개
// 블로그 현재 선택 로직 (@247-251):
//   const byIndustry = SEASONAL_TOPICS[brand.industry] ?? SEASONAL_TOPICS.restaurant;
//   const base = byIndustry[season.seasonKey] ?? byIndustry.spring;
//   return base.map((p) => ({ ...p, title: localizeTopic(p.title, brand.city) }));
// presets/localizeTopic/BODY_TONE 는 BlogScreen 에 그대로 남는다(무손상).
```

From briq-app/components/dashboard/Dashboard.tsx:
```typescript
// type SuggestedMove = { id: string; title: string; desc: string; kind?: string };  (@323)
// function suggestionsForIndustry(industry, brandName): SuggestedMove[]  (@325) — 순수함수, 모듈 레벨
//   호출처: SuggestedMoves(@258) → React.useMemo(() => suggestionsForIndustry(...), [industry, name])
//   event 무브(@352-357): title "이번 시즌 이벤트", kind "시즌",
//     desc static "어버이날·여름 휴가·크리스마스 등 시즌별 자연스러운 이벤트 안내. 광고티 없이."
```
</interfaces>

<source_audit>
GOAL: 시즌 인식을 카드뉴스·대시보드로 확장 + 시즌 주제 단일 소스화 → COVERED (Task 1/2/3)
REQ SEASON-SHARE: 공유 lib 추출 + 블로그 회귀 0 → COVERED (Task 1)
REQ CARDNEWS-SEASON: placeholder 버그 수정 + 시즌화 → COVERED (Task 2)
REQ DASH-SEASON: 이번 시즌 이벤트 시즌 인식 → COVERED (Task 3)
무손상(범위 밖, 건드리지 않음): lib/cardnews/campaign-recipes.ts, lib/dummy/recommendations.ts(dead),
  CardnewsScreen getContext()/ctx/ctxLabel(GPT 컨텍스트용), BlogScreen presets/localizeTopic.
</source_audit>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SEASONAL_TOPICS 공유 lib 추출 + 블로그 전환(회귀 0)</name>
  <files>briq-app/lib/content/seasonal-topics.ts, briq-app/components/blog/BlogScreen.tsx</files>
  <action>
1. 신규 파일 `briq-app/lib/content/seasonal-topics.ts` 생성:
   - 상단 주석: "시즌×업종 추천 주제 단일 소스. 블로그·카드뉴스가 공유. 가짜 수치·통계 금지(정직성)."
   - `import { getSeasonContext, type SeasonKey } from "./season";`
   - `export type Topic = { title: string; keywords: string[]; intent: string };`
   - `export const SEASONAL_TOPICS: Record<string, Record<SeasonKey, Topic[]>> = { ... }`
     → BlogScreen.tsx @20-153 의 SEASONAL_TOPICS 객체 리터럴을 **글자 그대로** 옮긴다(내용 1자도
     변경 금지 — 회귀 방지). 업종 키 6개(restaurant/cafe/dessert/stay/beauty/local), 각 4시즌×3주제.
   - 함수 추가:
     ```typescript
     // 업종 × 시즌 추천 주제. season 미지정 시 현재 KST 시즌. 업종 미존재 시 restaurant 폴백.
     export function getSeasonalTopics(industry: string, season?: SeasonKey, now?: Date): Topic[] {
       const key = season ?? getSeasonContext(now).seasonKey;
       const byIndustry = SEASONAL_TOPICS[industry] ?? SEASONAL_TOPICS.restaurant;
       return byIndustry[key] ?? byIndustry.spring;
     }
     // placeholder 용 제목만.
     export function getSeasonalTopicTitles(industry: string, season?: SeasonKey, now?: Date): string[] {
       return getSeasonalTopics(industry, season, now).map((t) => t.title);
     }
     ```
   - 주의: 블로그의 폴백은 `byIndustry[season.seasonKey] ?? byIndustry.spring` 였다 →
     `getSeasonalTopics` 의 시즌 폴백을 `?? byIndustry.spring` 으로 **동일하게** 맞춘다(회귀 0).
     업종 폴백도 블로그와 동일하게 `SEASONAL_TOPICS.restaurant`.

2. BlogScreen.tsx 수정:
   - @16 `type Topic = ...` 로컬 선언 제거.
   - @18-153 의 `const SEASONAL_TOPICS = { ... }` 전체 블록 제거(주석 @18-19 포함).
   - @14 import 줄(`getSeasonContext`) 아래에 추가:
     `import { getSeasonalTopics, type Topic } from "@/lib/content/seasonal-topics";`
   - @247-251 presets useMemo 를 SEASONAL_TOPICS 직접 참조 → `getSeasonalTopics(...)` 로 교체:
     ```typescript
     const presets = React.useMemo(() => {
       const base = getSeasonalTopics(brand.industry, season.seasonKey);
       return base.map((p) => ({ ...p, title: localizeTopic(p.title, brand.city) }));
     }, [brand.industry, brand.city, season.seasonKey]);
     ```
   - `Topic` 가 BlogScreen 다른 곳에서 쓰이면 import 한 것으로 충족(동일 shape).
   - localizeTopic / presets map / activePreset / BODY_TONE 등 나머지 로직 **무손상**.
   - 결과적으로 블로그 추천 주제·키워드·intent·치환 결과가 추출 전과 **완전히 동일**해야 한다.
  </action>
  <verify>
    <automated>cd briq-app && ./node_modules/.bin/tsc --noEmit -p tsconfig.json 2>&1 | tail -5 && ./node_modules/.bin/jiti scripts/probe-d9z-1.ts</automated>
  </verify>
  <done>tsc 0 에러. PROBE-OK 출력(6월=여름·12월=겨울, 6업종×4시즌 각 ≥2, stay summer 회귀 동일, 가짜수치 0). BlogScreen 에 로컬 SEASONAL_TOPICS·로컬 Topic 선언 없음.</done>
</task>

<task type="auto">
  <name>Task 2: CardnewsScreen topicPlaceholder 버그 수정 + 시즌화</name>
  <files>briq-app/components/cardnews/CardnewsScreen.tsx</files>
  <action>
1. import 추가 (@45 `import { getContext } from "@/lib/viral/context";` 인접 줄에):
   `import { getSeasonalTopicTitles } from "@/lib/content/seasonal-topics";`

2. @356-437 의 `topicPlaceholder` useMemo 전체(버그 월 매트릭스 `tableByIndustry` 포함)를 교체:
   ```typescript
   // 현재 KST 시즌 × 업종 추천 주제 3개를 placeholder 로. (블로그와 동일 단일 소스)
   const topicPlaceholder = useMemo(() => {
     const titles = getSeasonalTopicTitles(brand.industry); // season 미지정 → 현재 KST 시즌
     if (titles.length === 0) return "예: 신메뉴 안내 · 예약 안내 · 이벤트 안내";
     return `예: ${titles.slice(0, 3).join(" · ")}`;
   }, [brand.industry]);
   ```
   - 3개 미만이면 slice(0,3) 가 안전(있는 만큼만), 0개면 위 폴백.
   - 기존 `new Date().getMonth()` 월 인덱싱·`(idx+1)%12`·`tableByIndustry` 전부 제거 → 10~12월 undefined 버그 해소.

3. **무손상 확인(변경 금지):**
   - @354 `ctx = useMemo(() => getContext(), [])` 그대로.
   - @439-450 `ctxLabel` (ctx.season="fall" 매핑 포함) 그대로 — placeholder 교체와 무관, GPT 컨텍스트 배지용.
   - getContext/ctx 는 시즌 placeholder 와 별개이므로 건드리지 않는다.
  </action>
  <verify>
    <automated>cd briq-app && ./node_modules/.bin/tsc --noEmit -p tsconfig.json 2>&1 | tail -5; echo "--- tableByIndustry must be 0:"; grep -c "tableByIndustry" components/cardnews/CardnewsScreen.tsx; echo "--- getSeasonalTopicTitles must be >=1:"; grep -c "getSeasonalTopicTitles" components/cardnews/CardnewsScreen.tsx</automated>
  </verify>
  <done>tsc 0 에러. tableByIndustry 0건, getSeasonalTopicTitles ≥1건. (런타임 확인은 verification 섹션 스크린샷에서.)</done>
</task>

<task type="auto">
  <name>Task 3: Dashboard "이번 시즌 이벤트" 시즌 인식</name>
  <files>briq-app/components/dashboard/Dashboard.tsx</files>
  <action>
1. import 추가 (@20 `import { cn } from "@/lib/utils";` 인접 줄에):
   `import { getSeasonContext } from "@/lib/content/season";`

2. @325 `suggestionsForIndustry` 함수 본문 상단(generic 배열 정의 전)에 현재 시즌 컨텍스트와
   시즌별 예시 1~2개 매핑을 추가:
   ```typescript
   const { seasonKey, seasonLabel } = getSeasonContext();
   const seasonHint: Record<"spring" | "summer" | "autumn" | "winter", string> = {
     spring: "봄나들이·어버이날",
     summer: "여름 휴가·장마",
     autumn: "추석·단풍",
     winter: "연말·크리스마스",
   };
   ```
   - SeasonKey 타입을 직접 import 하지 않고 위처럼 인라인 Record 키로 4개 고정(타입 안전).
   - getSeasonContext 는 순수·결정론(KST)이라 함수 내 호출 무방. (SuggestedMoves 의 useMemo 가
     [industry, name] 의존이라 마운트~리렌더 시 재계산 — 시즌은 거의 안 바뀌므로 충분.)

3. @352-357 event 무브 객체를 교체:
   ```typescript
   {
     id: "event",
     title: `${seasonLabel} 시즌 이벤트`,
     kind: "시즌",
     desc: `${seasonLabel} 제철·이벤트(${seasonHint[seasonKey]})를 광고티 없이 자연스럽게 안내.`,
   },
   ```
   - static "어버이날·여름 휴가·크리스마스" 단독 나열 제거.
   - kind "시즌" 유지(라우팅·HOOK_SEASON 무손상).
   - 가짜 수치 금지(예시 단어만, %·명·배 등 통계 토큰 사용 금지).

4. **무손상 확인:** 나머지 5개 무브(new-menu/reservation/review-repost/trend/revisit), pickMove 라우팅,
   Masthead 의 new Date(@383)·날짜 표기 그대로.
  </action>
  <verify>
    <automated>cd briq-app && ./node_modules/.bin/tsc --noEmit -p tsconfig.json 2>&1 | tail -5; echo "--- getSeasonContext must be >=1:"; grep -c "getSeasonContext" components/dashboard/Dashboard.tsx; echo "--- static 어버이날·여름 휴가·크리스마스 line must be 0:"; grep -c "어버이날·여름 휴가·크리스마스" components/dashboard/Dashboard.tsx</automated>
  </verify>
  <done>tsc 0 에러. getSeasonContext ≥1건, 기존 static "어버이날·여름 휴가·크리스마스" 문자열 0건. event desc 가 시즌 동적.</done>
</task>

</tasks>

<verification>
실행 전 Task 1 verify 용 jiti 프로브 스크립트를 `briq-app/scripts/probe-d9z-1.ts` 로 생성한다
(@/* alias 가 briq-app 루트 기준이므로 import 동작). 검증 후 삭제 가능.

프로브 내용:
```typescript
import { getSeasonalTopics, getSeasonalTopicTitles, SEASONAL_TOPICS } from "@/lib/content/seasonal-topics";
const inds = ["restaurant", "cafe", "dessert", "stay", "beauty", "local"];
let ok = true;
// (a) 6월→여름, 12월→겨울 (now 주입; KST 보정 위해 03:00Z 사용)
const jun = getSeasonalTopicTitles("stay", undefined, new Date("2026-06-15T03:00:00Z"));
const dec = getSeasonalTopicTitles("stay", undefined, new Date("2026-12-15T03:00:00Z"));
if (!jun.some((t) => t.includes("여름"))) { console.log("FAIL jun-summer", jun); ok = false; }
if (!dec.some((t) => t.includes("겨울") || t.includes("연말"))) { console.log("FAIL dec-winter", dec); ok = false; }
// (b) 6업종 × 4시즌 각 >=2
for (const i of inds) for (const s of ["spring", "summer", "autumn", "winter"] as const) {
  if (getSeasonalTopics(i, s).length < 2) { console.log("FAIL coverage", i, s); ok = false; }
}
// (c) 블로그 회귀: stay summer 제목이 SEASONAL_TOPICS 원본과 동일
const raw = SEASONAL_TOPICS.stay.summer.map((t) => t.title);
const got = getSeasonalTopics("stay", "summer").map((t) => t.title);
if (JSON.stringify(raw) !== JSON.stringify(got)) { console.log("FAIL regress", raw, got); ok = false; }
// (d) 가짜 수치 부재
const all = inds.flatMap((i) => (["spring", "summer", "autumn", "winter"] as const).flatMap((s) => getSeasonalTopics(i, s).map((t) => t.title)));
const bad = all.filter((t) => /\d+\s*(%|명|배|위|만\s*명)/.test(t));
if (bad.length) { console.log("FAIL fake-number", bad); ok = false; }
console.log(ok ? "PROBE-OK" : "PROBE-FAIL");
```

전체 게이트:
1. `cd briq-app && ./node_modules/.bin/tsc --noEmit -p tsconfig.json` → 0 에러.
2. Task 1 프로브 → PROBE-OK.
3. dev 서버(3000) 헤드리스 스크린샷:
   - `/cardnews` 주제 입력 placeholder 가 여름 결("예: …여름…" / 업종별 여름 주제 3개, undefined 없음).
   - `/dashboard` "이번 시즌 이벤트"(또는 "여름 시즌 이벤트") desc 가 여름 결("여름 제철…(여름 휴가·장마)…"),
     static "어버이날" 단독 노출 없음.
   - `/blog` 추천 주제가 변경 전과 동일(여름 결, 회귀 없음) 육안 확인.
4. 워크트리 미사용. 변경은 원자 커밋.
</verification>

<success_criteria>
- tsc 0 에러.
- 시즌 추천 주제 단일 소스(seasonal-topics.ts)에서 블로그·카드뉴스 공통 사용.
- 블로그 추천 결과 회귀 0(stay summer 등 프로브로 동일 검증).
- 카드뉴스 placeholder: 6월=여름 주제 3개, 10~12월 undefined 버그 해소(tableByIndustry 0건).
- 대시보드 event: 6월=여름 desc("여름 제철…(여름 휴가·장마)…"), static "어버이날" 단독 제거.
- 무손상: campaign-recipes.ts, recommendations.ts(dead), CardnewsScreen getContext/ctxLabel, BlogScreen localizeTopic/presets.
- 가짜수치·AI클리셰 없음.
</success_criteria>

<output>
After completion, create `.planning/quick/260608-d9z-seasonal-expand/260608-d9z-SUMMARY.md`
</output>
