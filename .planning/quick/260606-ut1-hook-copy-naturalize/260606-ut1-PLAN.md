---
phase: quick-260606-ut1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - briq-app/lib/cardnews/hook-generator.ts
  - briq-app/lib/cardnews/hook-patterns.ts
autonomous: true
requirements:
  - QUICK-260606-ut1
must_haves:
  truths:
    - "카드뉴스 HOOK 표제 출력에 근거 없는 가짜 수치('+2.4배', '1년에 60일', '저장 상위 N곳')가 없다"
    - "시즌 HOOK에 timeWord+subject 시즌어 중복('여름 한 번뿐인 초여름...')이 없다"
    - "전보문체 조각('자리 한정, 빠르게')이 자연스러운 사장님 구어체 문장으로 바뀌었다"
    - "tsc --noEmit 0건, 변수 치환/조사 헬퍼/줄바꿈/배열 구조/시그니처 무손상"
  artifacts:
    - path: "briq-app/lib/cardnews/hook-generator.ts"
      provides: "HOOK_NEW_MENU / HOOK_SEASON / HOOK_TREND 정직성·자연스러움 보정"
      contains: "HOOK_SEASON"
    - path: "briq-app/lib/cardnews/hook-patterns.ts"
      provides: "numeric 패턴 가짜 기본 숫자 제거"
      contains: "HOOK_PATTERNS_BY_TYPE"
  key_links:
    - from: "briq-app/lib/cardnews/hook-generator.ts"
      to: "slides[0] (hook 표제)"
      via: "hookPoolFor → pick"
      pattern: "hookPoolFor"
---

<objective>
카드뉴스 자동 생성 HOOK(표제) 풀에서 (1) 근거 없는 가짜 수치, (2) timeWord+subject 시즌어 중복, (3) 전보문체 조각을 사장님 구어체로 리라이트한다. 모든 old/new가 본 PLAN에 확정되어 있으므로 실행자는 발명 없이 그대로 적용만 한다.

Purpose: CLAUDE.md "정직성"(출처 없는 단정 수치 금지) + "카피 톤"(AI 광고체·전보문체·번역투 금지) 철칙 위반 교정. 카피 품질이 핵심 가치인 프로젝트에서 가짜 통계는 신뢰·법적 리스크.
Output: hook-generator.ts 4건 + hook-patterns.ts 1건 = 총 5라인 교체. 가짜수치 3종("+2.4배", "1년에 60일", "저장 상위 N곳") 제거.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md

# 핵심 규칙 (CLAUDE.md):
# - 정직성: 미검증 수치·가짜 고객/후기 금지. 시연 데이터는 "예시 화면" 라벨.
# - 카피 톤: AI 클리셰·전보문체·번역투 금지 → 사장님 구어체.
# - 한글 가짜 이탤릭 금지(이번 작업은 표제 텍스트만, 스타일 무관).
# - 검증: ./node_modules/.bin/tsc --noEmit -p tsconfig.json → 0.

<interfaces>
<!-- 실행자가 알아야 할 계약. 추가 탐색 불필요. -->

Ctx 타입 (hook-generator.ts:245):
  Ctx.v = Vocab (city, catShort, unit, signature, customerWord, slot, purchaseAction ...)
  Ctx.t = TopicTokens (subject?, timeWord?, limitWord?, number?)
  Ctx.brand = Brand, Ctx.kind, Ctx.topic

HookCtx 타입 (hook-patterns.ts:130): v(city,catShort,signature,customerWord,experienceWord,process) / t(subject?,timeWord?,number?)

조사 헬퍼: 은/이/을 (korean-particles) — 본 작업 대상 라인엔 이미 없거나 그대로 보존.
모든 HOOK 함수는 ((c) => string) 시그니처. \n 1개로 2줄 표제. 배열 항목 순서·개수 보존.

치환 예측 (lib/dummy/brands.ts):
  miokdang: restaurant/강남/campaign "5월 봄나물 코스" → catShort "한정식", signature "점심 코스", unit "코스"
  서촌 stay: catShort "한옥스테이", unit "1박"
  dolce dessert/성수: catShort "디저트", unit "케이크", signature "시즌 케이크"
  luna beauty/강남: catShort "헤어", unit "시술"
  시즌 캠페인 topic "여름 수박 케이크" → timeWord "여름", subject "수박 케이크"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: hook-generator.ts — 가짜수치·중복·전보문체 4라인 리라이트</name>
  <files>briq-app/lib/cardnews/hook-generator.ts</files>
  <action>
아래 4건을 정확히 old_string → new_string 으로 Edit. 각 old_string은 파일 내 유일 매칭. 변수자리·\n·배열 콤마·들여쓰기(공백 2칸) 보존.

──────────────────────────────────────────────
[1] 라인 263 (HOOK_NEW_MENU) — 가짜 숫자 "1년에 60일" 제거
근거: 브랜드 무관 임의 숫자(신메뉴 후크에 "60일"의 근거 없음). 시즌 희소성 표현으로 일반화.

old_string:
  (c) => `1년에 60일,\n그 60일이 시작됐습니다. ${c.t.subject || c.v.signature}.`,

new_string:
  (c) => `제철에만 잠깐 나오는,\n${c.t.subject || c.v.signature}.`,

──────────────────────────────────────────────
[2] 라인 283 (HOOK_SEASON) — timeWord+subject 시즌어 중복 + 전보문체 "자리 한정, 빠르게"
근거: 실제 출력 "여름 한 번뿐인 초여름 보양 한 상. 자리 한정, 빠르게."에서 timeWord(여름)와 subject 속 시즌어(초여름) 중복 + 전보체. timeWord 선행 제거하고 자리 안내를 자연 문장으로.

old_string:
  (c) => `${c.t.timeWord ?? "이번 시즌"} 한 번뿐인 ${c.t.subject || c.v.unit}.\n자리 ${c.t.limitWord ?? "한정"}, 빠르게.`,

new_string:
  (c) => `지금 아니면 다음 시즌인 ${c.t.subject || c.v.unit},\n자리 미리 잡아두시면 좋아요.`,

──────────────────────────────────────────────
[3] 라인 286 (HOOK_SEASON) — 가짜 통계 "+2.4배" 제거 (정직성 최우선)
근거: "예약 평소 대비 +2.4배"는 근거 없는 가짜 통계(CLAUDE.md 정직성 위반). 단정 배수 제거, 사장님 구어체.

old_string:
  (c) => `${c.t.timeWord ?? "어버이날"} 직전 주\n예약 평소 대비 +2.4배. 미리 잡으세요.`,

new_string:
  (c) => `${c.t.timeWord ?? "어버이날"} 가까워지면\n예약이 일찍 차요. 미리 잡아두세요.`,

──────────────────────────────────────────────
[4] 라인 327 (HOOK_TREND) — 가짜 랭킹 지표 "저장 상위 N곳" 제거
근거: "저장 상위 3곳"은 측정 근거 없는 가짜 랭킹 메트릭(저장수 기준 상위라는 단정). 사장님 구어체 안내로.

old_string:
  (c) => `${c.v.city} ${c.v.catShort} 저장 상위 ${c.t.number ?? "3"}곳,\n오늘 알려드립니다.`,

new_string:
  (c) => `${c.v.city}에서 ${c.v.catShort} 찾으실 때\n참고하기 좋은 곳, 오늘 정리했어요.`,
──────────────────────────────────────────────

[보존 판단 — 수정 금지]
- 라인 259 "100곳 중 ... 1곳" : 흔한 관용 수사(실측 통계 단정 아님). 보존.
- 라인 337 HOOK_EVENT "선착순 N분" : 정당한 한정 안내. 보존.
- 그 외 자연스러운 후크 다수: 과교정 금지, 손대지 않음.
  </action>
  <verify>
    <automated>cd briq-app && ./node_modules/.bin/tsc --noEmit -p tsconfig.json && grep -n "2.4배\|1년에 60일\|자리 한정, 빠르게\|저장 상위" lib/cardnews/hook-generator.ts; test $? -ne 0 && echo "OK: 가짜수치 부재"</automated>
  </verify>
  <done>tsc 0건. grep으로 "+2.4배"/"1년에 60일"/"자리 한정, 빠르게"/"저장 상위" 매칭 0. 4라인 모두 새 문구로 교체됨.</done>
</task>

<task type="auto">
  <name>Task 2: hook-patterns.ts — numeric 패턴 가짜 기본 숫자 "60" 제거</name>
  <files>briq-app/lib/cardnews/hook-patterns.ts</files>
  <action>
1건 Edit. numeric[0]의 fallback "60"은 토픽에 실제 숫자 토큰이 없을 때(대부분의 토픽) 발사되는 근거 없는 임의 숫자. 라인 263과 동일한 가짜수치 문제. number 토큰 의존 대신 시즌 희소성 표현으로 일반화한다(numeric 배열엔 [1] "100곳 중", [2] "1년에 딱 한 시즌"이 남아 패턴 성격 유지).

근거: `??"60"` 기본값은 검증 불가 임의 숫자. 단정 숫자 없이 "지금뿐" 희소성만 전달.

old_string:
    (c) => `1년에 ${c.t.number ?? "60"}일,\n그 ${c.t.number ?? "60"}일이 시작됐습니다.`,

new_string:
    (c) => `1년 중 잠깐 나오는,\n${c.t.subject || c.v.signature}.`,

[보존 판단 — 수정 금지]
- numeric[1] 라인 157 "100곳 중 ... 한 곳" : 관용 수사, 보존.
- numeric[2] 라인 158 "1년에 딱 한 시즌" : 단정 숫자 없는 정당 표현, 보존.
- HookPattern 타입 doc(라인 21,156 주석)의 "1년에 60일" 예시 문구는 주석이라 출력 영향 없음 — 손대지 않음(선택). 단 코드 라인만 위 1건 교체.
  </action>
  <verify>
    <automated>cd briq-app && ./node_modules/.bin/tsc --noEmit -p tsconfig.json && grep -n '`1年\|c.t.number ?? "60"' lib/cardnews/hook-patterns.ts; test $? -ne 0 && echo "OK: 60 fallback 제거"</automated>
  </verify>
  <done>tsc 0건. `c.t.number ?? "60"` 코드 매칭 0(주석 예시는 무관). numeric[0]이 희소성 문구로 교체됨.</done>
</task>

</tasks>

<verification>
1. 타입: `cd briq-app && ./node_modules/.bin/tsc --noEmit -p tsconfig.json` → 0.

2. jiti 프로브로 6업종 × 캠페인 종류별 실제 출력 점검(실행자가 임시 .mjs 작성, alias 필수):
   ```
   import { createJiti } from "jiti";
   import path from "node:path";
   import { fileURLToPath } from "node:url";
   const __filename = fileURLToPath(import.meta.url);
   const __dirname = path.dirname(__filename);
   const jiti = createJiti(__filename, { alias: { "@": path.resolve(__dirname, "briq-app") } });
   const { generateCardnewsCampaign } = await jiti.import("@/lib/cardnews/hook-generator");
   const { BRANDS } = await jiti.import("@/lib/dummy/brands"); // export 명 확인 후 맞게
   ```
   (export 이름이 다르면 `grep -n "export" lib/dummy/brands.ts`로 확인 후 맞춘다. generateCardnewsCampaign 시그니처도 grep로 확인.)
   - miokdang/roastery-1985/dolce-dessert/luna-hair/seochon-stay/forum-fashion × 토픽(신메뉴/시즌/예약/단골/리뷰/트렌드/이벤트)으로 호출, 각 slides[0](hook 표제) 출력.
   - assert(정규식): 출력 전체에 `+2.4배`, `1년에 60일`, `자리 한정, 빠르게`, `저장 상위`, /(\S+)\s한 번뿐인.*초여름/ 류 중복 부재.
   - 육안: 새 표제 4종이 변수 치환 후 자연스러운지(예: "여름 가까워지면 / 예약이 일찍 차요"는 시즌 캠페인에서 timeWord="여름"으로 자연).

3. (선택) dev 3000 + 헤드리스 Chrome 스크린샷으로 /cardnews 미리보기 표제 육안.
</verification>

<success_criteria>
- hook-generator.ts 4라인 + hook-patterns.ts 1라인 교체 완료(총 5라인).
- 가짜수치 3종 제거: "+2.4배"(286), "1년에 60일"(263 + patterns 156 fallback), "저장 상위 N곳"(327).
- 중복/전보문체 1종 제거: "한 번뿐인 ...초여름 / 자리 한정, 빠르게"(283).
- tsc 0건. 변수 치환·조사·\n·배열·시그니처 무손상. 영문 검색어 빌더·로직·과금·발행·이미지 무손상.
- 과교정 없음(259 100곳/337 선착순/기타 자연 후크 보존).
</success_criteria>

<output>
After completion, create `.planning/quick/260606-ut1-hook-copy-naturalize/260606-ut1-SUMMARY.md`
</output>
